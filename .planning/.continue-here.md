---
context: between_phases
phase: 01
phase_status: complete
next_phase: 02
total_tasks: 4
status: phase_1_complete_paused_before_phase_2
last_updated: 2026-05-15T18:38:39.752Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. The constraint below was discovered through failure during Phase 1 V-4 verification.
> Acknowledge it explicitly before starting Phase 2 work.

- [ ] CONSTRAINT: **empirical-probes-on-head-element-ordering** — RESEARCH §5 prescribed a `<head>` ordering claim ("CDN script is defer-equivalent — reads `window.tailwind.config` when it executes, which is after the inline config script has set it") that was structurally checked via grep ordering assertions but never browser-verified. It shipped through plan-checker iteration 2 and into executor implementation. At V-4 manual sweep, the implementation threw `Uncaught ReferenceError: tailwind is not defined` and rendered the page with no Tailwind utilities applied. Cost: 1 extra fix commit (00a6b21) + 1 docs-correction commit (a9e5b35). **Structural mitigation:** Before any Phase 2 `<head>` reordering, or addition/relocation of a script tag that mutates or depends on globals (`window.tailwind`, etc.), browser-verify the post-edit file via `python3 -m http.server` + DevTools console — zero JS errors AND visible identity matches baseline — BEFORE the plan locks. Grep ordering assertions are necessary but NOT sufficient for runtime-behavior claims.

**Do not proceed until this box is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| **empirical-probes-on-head-element-ordering** | Plan/RESEARCH made a present-tense empirical claim about runtime script ordering (`"CDN is defer-equivalent"`) without ever loading the post-edit file in a browser. Plan-checker iteration 2 + automated grep `<verify>` blocks passed; V-4 manual sweep caught the runtime ReferenceError. | blocking | Phase 2 discuss/plan: any `<action>` that reorders `<head>` scripts MUST include a browser-load verification step (zero JS errors + identity-matches-baseline) in `<verify>`, executed BEFORE the plan-checker signs off. Grep ordering assertions stay as the structural pre-check; browser load is the runtime post-check. Both required for scripts that touch globals. |
| **plan-verify-regex-drift-from-plan-action** | Phase 1 plan Task 2's verify regex `grep -cE "rgb\(var\(--[a-z-]+-rgb\) / <alpha-value>\)"` used a literal single space before `/`, but the plan's prescribed AFTER-shape (RESEARCH §4) used aligned multi-space padding. Executor's correct implementation under-counted as 0 against the literal-single-space regex; only the looser `\s+/` form returned the expected 17. Plan-checker iteration 2 didn't catch the inconsistency between the plan's own prescription and the plan's own verify regex. | advisory | When authoring `<verify>` regex, derive directly from the `<action>` block's prescribed character sequence (paste, then replace variable parts with char classes). Use `\s+` for any whitespace that the action allows to be aligned/multi-space. Do not author the verify regex from memory or from an analogous-but-different RESEARCH excerpt. |
| **claude-code-worktree-spawn-base-quirk** | Claude Code's `Agent(isolation="worktree")` spawned the Phase 1 executor's worktree off `main` (04a9b0f) rather than off the orchestrator's HEAD (`rebrand-theming` @ e3a92fd). Executor self-corrected via `git reset --hard rebrand-theming` BEFORE any commits — safe in this case because the worktree branch had zero unique commits to lose. If a future executor commits before doing the base-check, the harness could lose work. | advisory | Phase 2 executor agents (which use the same gsd-executor template) already assert expected base in their first action — trust the check, but if drift is detected POST-commit, do NOT self-recover; surface to the user for manual reconciliation. |

<current_state>
Phase 1 (theming-architecture-foundation) is **complete and shipped**. Branch `rebrand-theming` at commit `ac1d6ad`, pushed to `origin/rebrand-theming` (tracking set). Working tree clean except for pre-existing untracked `.planning/COACH-STATE.md` (unrelated to Phase 1).

Pete's V-4 manual visual sweep passed on 2026-05-15. All seven validation hooks (V-1 through V-7) green — V-5 inferred from V-2's success on the same `<alpha-value>` substitution mechanism.

Next action is Phase 2 discuss-phase entry: `/gsd-discuss-phase 2`. No CONTEXT.md exists yet for phase 02; the discuss-phase workflow will gather it.
</current_state>

<completed_work>

**Phase 1 commit chain (9 commits total since `e3a92fd`):**

```
ac1d6ad docs(roadmap): mark Phase 1 + plan 01-01 complete (2026-05-15)
5b6b00d docs(state): mark Phase 1 complete, point to Phase 2 next
a9e5b35 docs(01): correct head-ordering claims in RESEARCH §5 + PATTERNS
02c9d79 chore: merge phase 1 executor worktree (plan 01-01)
e363ab0 docs(01-01): add Phase 1 plan-complete SUMMARY
00a6b21 fix(01-01): move inline Tailwind config after CDN script
3e3ae57 feat(01-01): wire FOUC script + reorder <head>
c312693 feat(01-01): rewrite inline Tailwind config to consume :root tokens
0129cc6 feat(01-01): add theme contract <style> with :root token defaults
```

**Final `<head>` ordering in `index.html`:**

| Pos | Element | Line | Source |
|-----|---------|------|--------|
| 1 | FOUC `<script>` | 7–12 | NEW (3e3ae57) |
| 2 | Token contract `<style>` | 13–84 (`:root` at 48) | NEW (0129cc6) |
| 3 | Tailwind CDN `<script>` | 85 | MOVED from line 7 on `main` |
| 4 | Inline Tailwind config | 86–123 | REWRITTEN (c312693) + POSITION FIXED (00a6b21) |
| 5 | Google Fonts `<link>` | 124 | MOVED, content byte-identical (D-16) |
| 6 | Existing component `<style>` | 125–706 | UNCHANGED (byte-identical to `main`) |
</completed_work>

<remaining_work>

**Phase 1: none** — fully shipped.

**Phase 2 (Overdrive Default Theme Migration) — not yet discussed/planned:**
- Retire dark hero (`--surface-dark-rgb`, `--surface-dark-card-rgb` tokens)
- Swap `--font-display: 'Fraunces', serif` → `'Space Grotesk', sans-serif`
- Migrate Category B (component `<style>` hex literals), Category C (inline-style hex in body markup), and Category D (stray non-tokenized `bg-indigo-*` / `text-amber-*` utilities)

Full Category B/C/D catalog with post-Phase-1 line numbers in `.planning/phases/01-theming-architecture-foundation/01-01-SUMMARY.md` § "Hand-off to Phase 2".
</remaining_work>

<decisions_made>

**Phase 1 ordering (corrected via 00a6b21):**
- Final ordering: FOUC → token `<style>` → CDN → inline config → fonts → component `<style>`
- Rationale: two simultaneous load-bearing constraints —
  1. Token `<style>` before CDN (cascade timing — Pitfall 2)
  2. CDN before inline config (JS dependency — `tailwind.config = {...}` requires `window.tailwind` which is created by CDN's IIFE; Pitfall 2b, surfaced during V-4)
- The pre-Phase-1 `main` ordering (`CDN → fonts → inline config`) implicitly satisfied #2 by having CDN first. Phase 1's original task-3 ordering placed inline config BEFORE CDN, which broke #2.

**No override blocks in Phase 1 (`:root` IS the Overdrive default):**
- D-02 + D-15 cascade fallback: unknown `?client=` slugs silently fall through to `:root` defaults
- No `[data-theme="overdrive"] { }` placeholder block — would break D-15

**Slate naming preserved in markup, semantic-neutral CSS-var slug:**
- D-07: ~80 `slate-*` Tailwind utility usages in markup stay untouched
- Tailwind config maps `slate.50` → `--neutral-50-rgb` (etc.) — the slate ↔ neutral translation lives only in the Tailwind config block, nowhere else
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order — for the resuming session)

1. **`.planning/phases/01-theming-architecture-foundation/01-01-SUMMARY.md`** — single source of truth for what Phase 1 shipped, the load-bearing RESEARCH §5 correction, and the Phase 2 hand-off catalog (Category B/C/D hex literals with post-Phase-1 line numbers).
2. **`.planning/phases/01-theming-architecture-foundation/01-RESEARCH.md`** — corrected (commit a9e5b35); read §5 + Pitfall 2 + new Pitfall 2b for the Phase 1 ordering rationale before any Phase 2 `<head>` change.
3. **`.planning/PROJECT.md`** — locked decisions (single-file, no dark backgrounds, theme contract = CSS vars, Tailwind via vars, structure/skin separation, theming visual only).
4. **`.planning/ROADMAP.md`** — Phase 2 scope + success criteria.
5. **`./index.html`** — current state of the theming contract; reference for what tokens exist (`:root` at line 48) and what utilities consume them.
6. **`~/.claude/lessons.md`** — most recent two entries (2026-05-15, plg-readiness-rebrand) capture the empirical-probes failure and the plan-verify-regex-drift defect.

## Infrastructure State

- **Git:** `rebrand-theming` @ `ac1d6ad`, tracking `origin/rebrand-theming`, ahead 0 behind 0. `main` worktree at `/Users/petergiordano/Documents/GitHub/plg-readiness` is still on `04a9b0f` (untouched).
- **Worktrees:** Phase 1 executor worktree removed; branch `worktree-agent-a683ac3df131c0573` deleted.
- **Local servers:** `python3 -m http.server 8080` (was serving `main`) and `:8081` (was serving the executor's now-removed worktree) — Pete confirmed both stopped.
- **Untracked:** `.planning/COACH-STATE.md` — pre-existing, NOT phase-1-related, leave alone unless Pete asks.

## Pre-Execution Critique Required

Not applicable — Phase 1 is complete, Phase 2 has not yet entered design/plan.

<context>
Phase 1 shipped after a V-4-caught runtime bug that traces back to an unverified empirical claim in RESEARCH §5. The GSD system worked exactly as designed: the human-verify gate (V-4) caught what automated checks (grep ordering assertions, plan-checker iterations) could not. The RESEARCH §5 framing has been corrected in-place (commit a9e5b35) so Phase 2's discuss/plan does not re-import the wrong premise. Two lessons captured in ~/.claude/lessons.md.

The cleanest entry into Phase 2 is `/gsd-discuss-phase 2`. The Phase 2 plan will likely touch multiple files (component `<style>` block in `index.html` PLUS the `<head>` for the font-token value swap), which is more surface than Phase 1's `<head>`-only scope. Worth a careful discuss-phase to surface any decisions about whether Category D stray utilities get rewritten or token-extended.
</context>

<next_action>
Start with: `/gsd-discuss-phase 2`

Before invoking, the resuming session should:
1. Read this `.continue-here.md` end-to-end and check the blocking-constraint box above.
2. Spot-check git state: `git status` + `git log --oneline -10` should match the commit chain in `<completed_work>`.
3. Confirm `index.html` lines 1–125 match the "Final `<head>` ordering" table above (FOUC at 7, `:root` at 48, CDN at 85, inline config at 86, fonts at 124, component `<style>` at 125).
4. Then invoke `/gsd-discuss-phase 2`.
</next_action>
