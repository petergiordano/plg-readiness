# Decisions (Synthesized Intel)

Extracted from classified ADRs. One entry per architectural decision with status, scope, and provenance.

---

_No ADRs in the current ingest set._

The settled constraints in `plan.md` are not formal ADRs — they are bundled in a PRD-style document under a "Locked decisions" section. Per the classifier note (`notes` field on `plan-a7f3c2e1.json`), those bullets are surfaced as `kind: locked-constraint` requirements in `requirements.md` rather than as ADRs. If the project owner later wants any of them promoted to formal ADRs (with their own status / consequences sections), that is a separate step downstream of synthesis.

source: (no ADR sources in ingest set)
