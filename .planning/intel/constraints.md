# Constraints (Synthesized Intel)

Extracted from classified SPECs. One entry per constraint with type (api-contract | schema | nfr | protocol) and provenance.

---

_No SPECs in the current ingest set._

The technical constraints implied by `plan.md` (CSS-variable theme contract, Tailwind token wiring, `data-theme` switch mechanism) are captured as `kind: locked-constraint` requirements in `requirements.md` because they originate from a PRD-shaped document, not a standalone spec. If a separate SPEC doc is added later (e.g. a formal "theme contract spec" enumerating every required token), it would be ingested here.

source: (no SPEC sources in ingest set)
