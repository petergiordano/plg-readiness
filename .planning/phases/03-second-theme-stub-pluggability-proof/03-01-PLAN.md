---
phase: 03-second-theme-stub-pluggability-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements: [REQ-stub-second-theme]

must_haves:
  truths:
    - "D-03: This plan is the WR-01 pre-cursor — a 1-task atomic fix landing BEFORE the Alchemist override block (Plan 03-02) is added, mirroring the 02-06 atomic gap-closure pattern. Fix-first sequencing keeps Plan 03-02 a clean 'add an override block' diff and eliminates the SC #2 frankenstein risk that would surface immediately under any non-Overdrive `--surface-elev-rgb` value."
    - "After this plan ships, no `background: white;` literal remains anywhere in index.html — every resting-state card background flows through the `--surface-elev-rgb` token contract."
    - "Under the bare URL (Overdrive default), `.answer-card` and `.check-card` resting backgrounds still render visually identical to pre-plan state (`rgb(255, 255, 255)` = white) because Overdrive's `--surface-elev-rgb` is `255 255 255` — zero visual regression on Overdrive."
    - "Under a future Alchemist load (`?client=alchemist`, delivered in Plan 03-02), the same cards will render Alchemist's `--surface-elev-rgb` value instead of stuck-on white — eliminating the SC #2 frankenstein risk that would otherwise surface immediately."
    - "Hover and selected states on both card types are NOT touched — they already flow through token contract (`--accent-rgb`, `--accent-muted-rgb`); regression-free."
  artifacts:
    - path: "index.html"
      provides: "Token-driven resting-state background on `.answer-card` (line 180) and `.check-card` (line 210), both reading `background: rgb(var(--surface-elev-rgb));`"
      contains: "background: rgb(var(--surface-elev-rgb))"
  key_links:
    - from: "index.html line 180 (`.answer-card` resting bg)"
      to: "`--surface-elev-rgb` token in `:root` (line 56)"
      via: "`rgb(var(--surface-elev-rgb))` CSS function call"
      pattern: "rgb\\(var\\(--surface-elev-rgb\\)\\)"
    - from: "index.html line 210 (`.check-card` resting bg)"
      to: "`--surface-elev-rgb` token in `:root` (line 56)"
      via: "`rgb(var(--surface-elev-rgb))` CSS function call"
      pattern: "rgb\\(var\\(--surface-elev-rgb\\)\\)"
---

<objective>
Close WR-01 from `02-REVIEW.md` via a two-line value flip on `index.html` lines 180 + 210. Replace the hardcoded `background: white;` literal on `.answer-card` (resting) and `.check-card` (resting) with `background: rgb(var(--surface-elev-rgb));` so both card surfaces honor the Phase 1 D-02 structure-skin separation contract.

Purpose: Eliminate the only known hardcoded-white card surface in the codebase BEFORE Plan 03-02 introduces the Alchemist override block. Under Overdrive (`--surface-elev-rgb: 255 255 255`), zero visual delta. Under Alchemist (any non-white `--surface-elev-rgb`), cards correctly render the active theme's elevated-surface color — avoiding the SC #2 "no element falls back to Overdrive defaults mid-page" failure mode that would otherwise surface immediately when Plan 03-02 activates `?client=alchemist`.

Output: One commit. Two single-line edits within the component `<style>` block of `index.html` (lines 130–277 region). No markup touched. No Tailwind config touched. No token contract touched. Mirrors the atomic gap-closure shape of `02-06-PLAN.md` Task 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-second-theme-stub-pluggability-proof/03-CONTEXT.md
@.planning/phases/03-second-theme-stub-pluggability-proof/03-RESEARCH.md
@.planning/phases/02-overdrive-default-theme-migration/02-REVIEW.md
@.planning/phases/02-overdrive-default-theme-migration/02-06-PLAN.md

<interfaces>
<!-- Current state of the WR-01 sites in index.html, lifted from re-grep at plan-write time (2026-05-16). Do not re-explore. -->

From index.html lines 168–183 (`.answer-card` block):
- Line 168: `.answer-card {`
- Line 174: `border: 1.5px solid rgb(var(--border-rgb));` (already token-driven — DO NOT TOUCH)
- Line 180: `background: white;` (WR-01 target — CHANGE TO `background: rgb(var(--surface-elev-rgb));`)
- Line 181: `}` (closing brace of `.answer-card`)
- Line 182: `.answer-card:hover { ... background: rgb(var(--accent-rgb) / 0.06); }` (already token-driven — DO NOT TOUCH)
- Line 183: `.answer-card.selected { ... background: rgb(var(--accent-muted-rgb)); }` (already token-driven — DO NOT TOUCH)

From index.html lines 201–216 (`.check-card` block):
- Line 201: `.check-card {`
- Line 206: `border: 1.5px solid rgb(var(--border-rgb));` (already token-driven — DO NOT TOUCH)
- Line 210: `background: white;` (WR-01 target — CHANGE TO `background: rgb(var(--surface-elev-rgb));`)
- Line 214: `}` (closing brace of `.check-card`)
- Line 215: `.check-card:hover { ... background: rgb(var(--accent-rgb) / 0.06); }` (already token-driven — DO NOT TOUCH)
- Line 216: `.check-card.selected { ... background: rgb(var(--accent-muted-rgb)); }` (already token-driven — DO NOT TOUCH)

From index.html line 56 (the `:root` token consumed by the fix):
- `--surface-elev-rgb:      255 255 255;  /* #FFFFFF white — consumed by bg-surface-elev (D-02) */`
- Under Overdrive: resolves to white. Under Alchemist (Plan 03-02): resolves to Alchemist's elevated-surface color.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip `background: white;` to `background: rgb(var(--surface-elev-rgb));` on lines 180 and 210</name>
  <files>index.html</files>
  <read_first>
    - index.html lines 168–216 (full `.answer-card` and `.check-card` CSS blocks — confirm lines 180 and 210 still hold `background: white;` before editing; if any drift has occurred, surface to the user and stop)
    - .planning/phases/02-overdrive-default-theme-migration/02-06-PLAN.md (Task 1 — the atomic gap-closure pattern this plan mirrors; especially the action/verify/acceptance_criteria structure)
    - .planning/phases/02-overdrive-default-theme-migration/02-REVIEW.md (WR-01 section — source-of-truth for the violation framing and the chosen fix)
    - .planning/phases/03-second-theme-stub-pluggability-proof/03-RESEARCH.md (Research Area 1 — confirms these are the ONLY two hardcoded `background: white` literals in the file, and that JetBrains Mono font literals on lines 190/243/249/262 are acceptable exceptions NOT in scope for this plan)
  </read_first>
  <action>
    Open index.html. Locate line 180 inside the `.answer-card { ... }` block (the resting state declaration, immediately after `text-align: left;` on line 179 and immediately before the closing `}` on line 181). The current line reads `            background: white;` (12-space leading indent, then `background: white;`). Replace with `            background: rgb(var(--surface-elev-rgb));` — preserve the existing 12-space leading indent exactly. Then locate line 210 inside the `.check-card { ... }` block (the resting state declaration, immediately after `transition: border-color 0.14s, background 0.14s;` on line 209 and immediately before `width: 100%;` on line 211). The current line reads `            background: white;` (same 12-space indent). Replace with `            background: rgb(var(--surface-elev-rgb));` — preserve the existing 12-space leading indent exactly.

    Do NOT touch:
    - Line 182: `.answer-card:hover { ... background: rgb(var(--accent-rgb) / 0.06); }` (already token-driven)
    - Line 183: `.answer-card.selected { ... background: rgb(var(--accent-muted-rgb)); }` (already token-driven)
    - Line 215: `.check-card:hover { ... background: rgb(var(--accent-rgb) / 0.06); }` (already token-driven)
    - Line 216: `.check-card.selected { ... background: rgb(var(--accent-muted-rgb)); }` (already token-driven)
    - Lines 190, 243, 249, 262: `font-family: 'JetBrains Mono', monospace;` literals (acceptable exceptions per 03-RESEARCH.md Category 2; out of scope for this plan)
    - The `:root` block (lines 48–85), the Tailwind config (lines 88–128), the Google Fonts `<link>` (line 129), any markup below line 277

    This is a two-line edit, both single-line replacements. File length stays at 1007 lines. Downstream line numbers are unaffected. Per project constraint (REQ-single-file-no-build), no build step or transpile follows — save the file.

    Commit message: `fix(03-01): replace hardcoded white resting bg on answer-card + check-card with surface-elev token (WR-01)`
  </action>
  <verify>
    <automated>grep -c 'background: white' /Users/petergiordano/Documents/GitHub/plg-readiness-rebrand/index.html</automated>
    <!-- Expected: 0 (both WR-01 sites retired) -->
    <automated>grep -c 'background: rgb(var(--surface-elev-rgb))' /Users/petergiordano/Documents/GitHub/plg-readiness-rebrand/index.html</automated>
    <!-- Expected: 2 (both resting-state rules now token-driven) -->
    <automated>grep -nE '\.answer-card\.selected|\.check-card\.selected' /Users/petergiordano/Documents/GitHub/plg-readiness-rebrand/index.html | grep -c 'accent-muted-rgb'</automated>
    <!-- Expected: 2 (selected states untouched — still reference --accent-muted-rgb) -->
    <automated>grep -nE '\.answer-card:hover|\.check-card:hover' /Users/petergiordano/Documents/GitHub/plg-readiness-rebrand/index.html | grep -c 'accent-rgb'</automated>
    <!-- Expected: 2 (hover states untouched — still reference --accent-rgb / 0.06) -->
    <automated>wc -l /Users/petergiordano/Documents/GitHub/plg-readiness-rebrand/index.html | awk '{print $1}'</automated>
    <!-- Expected: 1007 (file length unchanged — single-line replacements add/remove no lines) -->
    <automated>grep -nE '^\s+--surface-elev-rgb:' /Users/petergiordano/Documents/GitHub/plg-readiness-rebrand/index.html | wc -l | tr -d ' '</automated>
    <!-- Expected: 1 (the :root token declaration unchanged at line 56) -->
    <automated>python3 -m http.server 8765 --directory /Users/petergiordano/Documents/GitHub/plg-readiness-rebrand >/dev/null 2>&1 &amp; SERVER_PID=$!; sleep 1; curl -s -o /dev/null -w "%{http_code}" http://localhost:8765/index.html; kill $SERVER_PID 2>/dev/null</automated>
    <!-- Expected: 200 (file still serves; no syntax error introduced) -->
  </verify>
  <acceptance_criteria>
    - `grep -c 'background: white' index.html` returns exactly `0` (both WR-01 sites at lines 180 + 210 retired).
    - `grep -c 'background: rgb(var(--surface-elev-rgb))' index.html` returns exactly `2` (both resting-state rules now flow through the token contract).
    - `grep -nE '\.answer-card\.selected|\.check-card\.selected' index.html | grep -c 'accent-muted-rgb'` returns `2` (selected states on both cards untouched).
    - `grep -nE '\.answer-card:hover|\.check-card:hover' index.html | grep -c 'accent-rgb'` returns `2` (hover states on both cards untouched).
    - `wc -l index.html` returns `1007` (file length unchanged — confirms single-line replacements).
    - `grep -nE '^\s+--surface-elev-rgb:' index.html` returns exactly 1 line in the `:root` block (line 56, unchanged) — confirms the `:root` token contract was NOT touched.
    - `grep -cE "font-family: 'JetBrains Mono'" index.html` returns `4` (lines 190, 243, 249, 262 — acceptable exceptions untouched per 03-RESEARCH.md Category 2).
    - `grep -c '<style>' index.html` returns `2` (theme contract `<style>` + component `<style>` — both still present, no new `<style>` element introduced).
    - HTTP server returns `200` for `index.html` (file still parses and serves).
    - `git diff --stat index.html` shows the change confined to the component `<style>` block region (lines 130–277). No diff lines outside that range.
    - Browser-runtime assertion (executor SHOULD run this manually in DevTools after `python3 -m http.server 8080` and load `http://localhost:8080/`): `getComputedStyle(document.querySelector('.answer-card')).backgroundColor === 'rgb(255, 255, 255)'` returns `true` (Overdrive `--surface-elev-rgb` resolves to white, so zero visual delta from pre-plan state).
  </acceptance_criteria>
  <done>
    `index.html` lines 180 and 210 both now declare `background: rgb(var(--surface-elev-rgb));` instead of `background: white;`. All grep assertions pass. The file still serves over HTTP. Under the bare URL (Overdrive), answer cards and check cards render visually identical to pre-plan state (white). The codebase now contains ZERO hardcoded `background: white` literals — Plan 03-02 can introduce the Alchemist override block without producing a frankenstein white-on-teal card surface.
  </done>
</task>

</tasks>

<verification>
**Source-level proof WR-01 is closed (machine-verifiable, no browser required):**

```bash
# 1. Old white literals fully retired
grep -c 'background: white' index.html
# Expected: 0

# 2. Both resting-state declarations now token-driven
grep -c 'background: rgb(var(--surface-elev-rgb))' index.html
# Expected: 2 (one per card type)

# 3. File length unchanged (single-line replacements add/remove no lines)
wc -l index.html
# Expected: 1007

# 4. Token contract untouched (--surface-elev-rgb declaration unchanged in :root)
grep -nE '^\s+--surface-elev-rgb:' index.html
# Expected: 1 match at line 56

# 5. Hover + selected states untouched (regression guard)
grep -nE '\.answer-card:hover|\.check-card:hover' index.html
# Expected: 2 matches (lines 182, 215) both still reference --accent-rgb / 0.06
grep -nE '\.answer-card\.selected|\.check-card\.selected' index.html
# Expected: 2 matches (lines 183, 216) both still reference --accent-muted-rgb

# 6. Acceptable JetBrains Mono literals still present (NOT in scope, per 03-RESEARCH.md Category 2)
grep -cE "font-family: 'JetBrains Mono'" index.html
# Expected: 4 (lines 190, 243, 249, 262)
```

**Runtime proof (DevTools, after `python3 -m http.server 8080`, load `http://localhost:8080/`):**

1. `getComputedStyle(document.querySelector('.answer-card')).backgroundColor` returns `'rgb(255, 255, 255)'` (Overdrive `--surface-elev-rgb` is `255 255 255` — zero visual delta from pre-plan state).
2. `getComputedStyle(document.querySelector('.check-card')).backgroundColor` returns `'rgb(255, 255, 255)'` (same reasoning).
3. Visual sanity (eye-check, no assertion): slide 1 answer cards and slide 4 check cards render visually identical to pre-plan main branch.

**Regression-prevention proof (no scope creep):**

- `git diff --stat index.html` shows exactly 2 lines modified, both within the component `<style>` block (lines 130–277 region).
- The theme contract `<style>` (lines 13–86), Tailwind config (lines 88–128), Google Fonts `<link>` (line 129), FOUC `<script>` (lines 7–12), and all markup (lines 278+) are untouched.
- WR-02 through WR-06 from 02-REVIEW.md remain unaddressed in this plan — per CONTEXT.md they are confirmed NOT second-theme frankenstein risks and stay deferred to a future polish phase.
- The four JetBrains Mono `font-family` literals (lines 190, 243, 249, 262) are NOT touched per 03-RESEARCH.md Category 2 ("acceptable exceptions" — Alchemist does not override `--font-mono`, so these do not produce a frankenstein surface under Plan 03-02).
</verification>

<success_criteria>
**Plan-level success criteria (WR-01 closure):**

1. Zero `background: white;` literals remain in `index.html` (`grep -c 'background: white' index.html` returns `0`).
2. Both `.answer-card` and `.check-card` resting-state backgrounds flow through the `--surface-elev-rgb` token (`grep -c 'background: rgb(var(--surface-elev-rgb))' index.html` returns `2`).
3. File length unchanged at 1007 lines (single-line replacements).
4. Hover and selected states on both card types remain untouched (still reference `--accent-rgb` and `--accent-muted-rgb` respectively).
5. No edits to the theme contract `<style>` (lines 13–86), Tailwind config (lines 88–128), Google Fonts `<link>` (line 129), FOUC `<script>` (lines 7–12), or any markup.

**Phase-level downstream success criteria (when Plan 03-02 ships):**

6. Under `?client=alchemist`, `.answer-card` and `.check-card` resting backgrounds will render Alchemist's `--surface-elev-rgb` value (NOT stuck-on white), eliminating the SC #2 frankenstein risk identified in CONTEXT D-03.
7. Under the bare URL (Overdrive), card backgrounds render visually identical to pre-plan state — zero Phase 2 regression.

**Scope-boundary success criteria (no creep):**

8. WR-02 through WR-06 from `02-REVIEW.md` are NOT addressed (confirmed NOT second-theme frankenstein risks per CONTEXT.md; deferred to a future polish phase).
9. The Alchemist override block, Google Fonts edit, and VALIDATION rigs are NOT in scope for this plan — they ship in Plan 03-02.
10. The JetBrains Mono `font-family` literals on lines 190, 243, 249, 262 are NOT touched (acceptable exceptions per 03-RESEARCH.md Category 2; Alchemist does not override `--font-mono`).
</success_criteria>

<output>
After completion, create `.planning/phases/03-second-theme-stub-pluggability-proof/03-01-SUMMARY.md` documenting:

- The two-line value flip on `index.html` lines 180 + 210 (old `background: white;` → new `background: rgb(var(--surface-elev-rgb));`).
- Source-level grep evidence that WR-01 is closed (`grep -c 'background: white' index.html` → `0`; `grep -c 'background: rgb(var(--surface-elev-rgb))' index.html` → `2`).
- Confirmation that the file still serves over HTTP 200 and renders visually identical under the bare URL (Overdrive `--surface-elev-rgb` is white, so no visual delta).
- Explicit confirmation that WR-02 through WR-06 were NOT touched (list each by name and state "deferred per CONTEXT.md confirmation that these are Overdrive-internal cosmetic issues, not second-theme frankenstein risks").
- Explicit confirmation that the four JetBrains Mono `font-family` literals (lines 190, 243, 249, 262) were NOT touched (acceptable exceptions per 03-RESEARCH.md Category 2).
- A pointer to Plan 03-02 as the next plan in this phase (Alchemist override block + Google Fonts `<link>` edit + three VALIDATION scenarios).
- 1 atomic commit expected for this plan: the `index.html` two-line value flip.
</output>
</content>
</invoke>