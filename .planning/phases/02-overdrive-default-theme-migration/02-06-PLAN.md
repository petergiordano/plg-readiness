---
phase: 02-overdrive-default-theme-migration
plan: 06
type: execute
wave: 6
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05]
files_modified:
  - index.html
  - .planning/phases/02-overdrive-default-theme-migration/02-VALIDATION.md
autonomous: true
gap_closure: true
closes_gaps:
  - "BL-01 — `--neutral-50-rgb` and `--surface-rgb` both resolve to `#FFF8F0`, collapsing 7 `bg-slate-50` surfaces (4 reference-matrix `<th>` headers + 3 grid cards) into their `bg-surface-warm` parent."
requirements: [REQ-overdrive-default-theme]

must_haves:
  truths:
    - "A user opening index.html with no theme override sees the reference-matrix region of the results page render coherently — the 4 `<th>` table headers and 3 grid cards have visible fill differentiation from their `bg-surface-warm` parent `<section>`."
    - "Phase 2 SC #1 (Overdrive identity end-to-end across all six slides AND the results page) is fully VERIFIED — no PARTIAL-FAIL on the reference matrix region."
    - "Phase 2 SC #3 (internally coherent) holds in spirit — the cards-inside-section visual hierarchy is preserved, not collapsed."
    - "`--neutral-50-rgb` and `--surface-rgb` carry semantically distinct values in the `:root` token contract — they no longer hold the same RGB triplet."
    - "The next verification round can catch this class of regression — a validation rig now asserts that `bg-slate-50` consumers DIFFER from their `bg-surface-warm` parent at the computed-color level (not just that surfaces meant to be continuous ARE continuous)."
    - "Phase 1 D-09 (neutrals brandable per client) is preserved — the new `--neutral-50-rgb` value is still a `:root` token, still client-overridable."
    - "CONTEXT D-06 spirit (warm-gray ramp with warm undertone) is preserved — the new value `#FAF3E9` is still in the warm-gray family, one shade deeper than the surface anchor."
  artifacts:
    - path: "index.html"
      provides: "Re-anchored `--neutral-50-rgb` token (`250 243 233` / `#FAF3E9`) with an inline comment explaining the deliberate distinction from `--surface-rgb`"
      contains: "--neutral-50-rgb:  250 243 233"
    - path: ".planning/phases/02-overdrive-default-theme-migration/02-VALIDATION.md"
      provides: "Amended or added validation rig that asserts `bg-slate-50` consumer ≠ `bg-surface-warm` parent at computed-color level"
      contains: "bg-slate-50"
  key_links:
    - from: "index.html line 66 (`--neutral-50-rgb`)"
      to: "Tailwind `slate.50` config entry (lines 109)"
      via: "`rgb(var(--neutral-50-rgb) / <alpha-value>)` template"
      pattern: "var\\(--neutral-50-rgb\\)"
    - from: "Tailwind `bg-slate-50` utility"
      to: "7 consumer sites (lines 615, 616, 617, 618, 671, 680, 689)"
      via: "Tailwind JIT class resolution at runtime"
      pattern: "bg-slate-50"
    - from: "02-VALIDATION.md amended rig"
      to: "future BL-class regression detection"
      via: "computed-color delta assertion between `bg-slate-50` consumer and `bg-surface-warm` parent"
      pattern: "getComputedStyle.*backgroundColor"
---

<objective>
Close BL-01 from `02-VERIFICATION.md` via the single-line value flip recommended by `02-REVIEW.md` Option A — re-anchor `--neutral-50-rgb` from `255 248 240` to `250 243 233` so `bg-slate-50` consumers regain visual differentiation from their `bg-surface-warm` parent. Then amend the Phase 2 validation strategy so the next verification round can actually catch this class of regression (V-3 / V-10 currently assert continuity but not differentiation).

Purpose: Flip SC #1 from PARTIAL-FAIL to VERIFIED so Phase 2 can ship. Restore visible fill on 7 reference-matrix surfaces (4 `<th>` headers + 3 grid cards) without touching any markup, any Tailwind config, or any other token. Honor user's locked scope decision: BL-01 ONLY — no WR-01..06 work in this plan.

Output: One token-value edit + one comment update on line 66 of `index.html`. One validation-rig amendment in `02-VALIDATION.md` adding a computed-color delta assertion between a `bg-slate-50` sample point and its `bg-surface-warm` parent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-overdrive-default-theme-migration/02-CONTEXT.md
@.planning/phases/02-overdrive-default-theme-migration/02-VERIFICATION.md
@.planning/phases/02-overdrive-default-theme-migration/02-REVIEW.md
@.planning/phases/02-overdrive-default-theme-migration/02-VALIDATION.md

<interfaces>
<!-- Current token-contract state at line 66 of index.html, lifted from the verifier's read (do not re-explore). -->

From index.html lines 54-76 (the `:root` block region of interest):

- Line 55: `--surface-rgb:           255 248 240;  /* #FFF8F0 warm off-white */`
- Line 66: `--neutral-50-rgb:  255 248 240;   /* #FFF8F0 surface anchor */`  <-- THIS LINE CHANGES
- Line 67: `--neutral-100-rgb: 245 240 232;   /* #F5F0E8 warm-50 step */`  (unchanged; the new neutral-50 value `250 243 233` slots between `--surface-rgb` 255 248 240 and `--neutral-100-rgb` 245 240 232, preserving ramp monotonicity)

From index.html line 109 (Tailwind slate.50 entry — DO NOT TOUCH):
- `50:  'rgb(var(--neutral-50-rgb)  / <alpha-value>)',`

From index.html (consumer sites — DO NOT TOUCH MARKUP):
- Line 548: `bg-slate-50` inside `bg-surface-elev` (white) parent — third consumer, will read as a subtle warm tint after the flip (expected per the planning_context, no action)
- Lines 615, 616, 617, 618: 4 `<th>` headers using `bg-slate-50` inside `bg-surface-warm` parent `<section>` at line 602
- Lines 671, 680, 689: 3 `<div>` grid cards using `bg-slate-50 ... border border-slate-100` inside the same parent `<section>`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip `--neutral-50-rgb` value on line 66 of index.html and update its comment</name>
  <files>index.html</files>
  <read_first>
    - index.html lines 48-90 (the `:root` block — confirm line 66 still holds `--neutral-50-rgb:  255 248 240;` with the comment `/* #FFF8F0 surface anchor */`; if any drift has happened since the verifier read it, surface to the user and stop)
    - .planning/phases/02-overdrive-default-theme-migration/02-REVIEW.md (CR-01 section, especially the Option A code block at lines 57-62 — this is the source-of-truth for the chosen value `250 243 233` / `#FAF3E9`)
    - .planning/phases/02-overdrive-default-theme-migration/02-VERIFICATION.md (the `gaps:` frontmatter block AND the "BL-01" subsection in Gaps Summary — confirms the framing and the 7 affected sites)
    - .planning/phases/02-overdrive-default-theme-migration/02-CONTEXT.md lines 30-31 (D-06 warm-gray ramp anchors + D-07 keep full 10-stop ramp declared per Phase 1 D-09 brandable-per-client) — this is why the value MUST stay a `:root` token in the warm-gray family, not be replaced by a literal or removed
  </read_first>
  <action>
    Open index.html. Locate line 66 in the `:root` block inside the `<style>` element opened on or before line 48. The current line reads `--neutral-50-rgb:  255 248 240;   /* #FFF8F0 surface anchor */` (two spaces after the colon, two spaces between the RGB triplet and the trailing semicolon-comment pair). Replace the RGB triplet `255 248 240` with `250 243 233` and replace the trailing comment `/* #FFF8F0 surface anchor */` with `/* #FAF3E9 one step warmer-deeper than --surface-rgb; bg-slate-50 consumers must differentiate from bg-surface-warm parents */`. Preserve the existing leading indentation (12 spaces) and the existing internal column-alignment whitespace pattern used elsewhere in the ramp (lines 67-75 use one space after the colon, then the RGB triplet padded to column-align with neighbors, then three spaces, then the comment — match that pattern for the new value). Do NOT touch line 55 (`--surface-rgb`), do NOT touch line 67 (`--neutral-100-rgb`), do NOT touch line 109 (the Tailwind `slate.50` mapping), do NOT touch any markup site (lines 548, 615-618, 671, 680, 689). This is a single-line edit. No new tokens are added. No tokens are retired. Save the file. Per project skill, this is a single-file static web app — no build step, no transpile, no test runner.
  </action>
  <verify>
    <automated>grep -nE -- '--surface-rgb:[[:space:]]+255 248 240' index.html | wc -l | tr -d ' '</automated>
    <!-- Expected: 1 (line 55 unchanged) -->
    <automated>grep -nE -- '--neutral-50-rgb:[[:space:]]+250 243 233' index.html | wc -l | tr -d ' '</automated>
    <!-- Expected: 1 (line 66 changed to new value) -->
    <automated>grep -nE -- '--neutral-50-rgb:[[:space:]]+255 248 240' index.html | wc -l | tr -d ' '</automated>
    <!-- Expected: 0 (old neutral-50 value retired) -->
    <automated>grep -nE -- '#FAF3E9 one step warmer-deeper' index.html | wc -l | tr -d ' '</automated>
    <!-- Expected: 1 (new comment landed) -->
    <automated>grep -cE '^[[:space:]]*--neutral-[0-9]+-rgb:' index.html</automated>
    <!-- Expected: 10 (the full D-07 ramp is still declared end-to-end — no token retirement) -->
    <automated>python3 -m http.server 8765 >/dev/null 2>&1 &amp; sleep 1; curl -s -o /dev/null -w "%{http_code}" http://localhost:8765/index.html; kill %1 2>/dev/null</automated>
    <!-- Expected: 200 (file still parses and serves; smoke-check that no syntax error was introduced) -->
  </verify>
  <acceptance_criteria>
    - `grep -nE -- '--surface-rgb:[[:space:]]+255 248 240' index.html` returns exactly 1 match (line 55, unchanged).
    - `grep -nE -- '--neutral-50-rgb:[[:space:]]+250 243 233' index.html` returns exactly 1 match (line 66, new value).
    - `grep -nE -- '--neutral-50-rgb:[[:space:]]+255 248 240' index.html` returns 0 matches (old value retired).
    - `grep -nE -- '#FAF3E9 one step warmer-deeper than --surface-rgb' index.html` returns exactly 1 match (new comment present on line 66).
    - `grep -cE '^[[:space:]]*--neutral-[0-9]+-rgb:' index.html` returns 10 (full ramp still declared per D-07).
    - HTTP server returns 200 for `index.html` (file still parses).
    - No markup-site files changed — `git diff --stat index.html` shows the change scoped to the `:root` block region (a single line modified in the 48-85 line range plus no other diff sections).
    - No Tailwind config changes — `grep -n 'slate:' index.html` still shows the slate object at the same line it was on before this task, with the same mapping `50:  'rgb(var(--neutral-50-rgb)  / <alpha-value>)',`.
    - No new tokens added; no tokens retired. The `<head>` block line count is unchanged.
    - Browser-runtime assertion (executor MUST run this manually in DevTools after `python3 -m http.server 8080` and load `http://localhost:8080/`, then complete the quiz via any path to reach the results page): `getComputedStyle(document.documentElement).getPropertyValue('--neutral-50-rgb').trim() === '250 243 233'` returns `true`.
  </acceptance_criteria>
  <done>
    `index.html` line 66 now declares `--neutral-50-rgb:  250 243 233` with the inline comment explaining the deliberate distinction from `--surface-rgb`. All grep assertions pass. The file still serves over HTTP. The reference-matrix `<th>` headers and 3-up grid cards regain visible fill differentiation against their `bg-surface-warm` parent `<section>` (verified at the source level by the now-distinct RGB triplets; verified at runtime in step 1 of the next task's amended rig).
  </done>
</task>

<task type="auto">
  <name>Task 2: Amend 02-VALIDATION.md to add a `bg-slate-50` vs `bg-surface-warm` differentiation rig</name>
  <files>.planning/phases/02-overdrive-default-theme-migration/02-VALIDATION.md</files>
  <read_first>
    - .planning/phases/02-overdrive-default-theme-migration/02-VALIDATION.md (full file — to confirm V-3 and V-10 currently assert CONTINUITY of surface across body/section/footer at 4 sample points but do NOT assert DIFFERENTIATION of surfaces meant to differ; also to confirm the existing rig table format and the "Manual-Only Verifications" table format so the amendment slots in cleanly)
    - .planning/phases/02-overdrive-default-theme-migration/02-VERIFICATION.md (the "Gaps Summary" section, especially the paragraph beginning "Why this matters relative to the SUMMARY narrative" — articulates exactly why V-3 / V-10 missed BL-01 and is the source for the "unintended corollary" framing used in the new rig's rationale)
    - .planning/phases/02-overdrive-default-theme-migration/02-REVIEW.md (CR-01 — provides the concrete consumer site list, especially the 3-up grid card at line 671, which is the recommended sample point)
    - .planning/phases/02-overdrive-default-theme-migration/02-CONTEXT.md lines 30-31 (D-06 / D-07 — to understand why differentiation between `bg-slate-50` and `bg-surface-warm` is the load-bearing intent of the warm-gray ramp design, not a happy accident)
    - index.html lines 600-700 (the reference-matrix `<section>` at line 602 and the 3-up grid card at line 671 — to confirm the DOM-traversal path the executor will encode into the rig's `querySelector` chain)
  </read_first>
  <action>
    Open 02-VALIDATION.md. Add a new validation row labeled "V-11 — Surface-differentiation guard for `bg-slate-50` inside `bg-surface-warm`" immediately AFTER the existing V-10 block and BEFORE the existing "V-11 (optional) — `VM338:1 Uncaught SyntaxError` noise diagnosis" block (i.e., the optional V-11 row becomes V-12 after this insertion). Note: there is already a V-11 numbered slot in the file used for the optional Tailwind CDN noise diagnosis — renumber that existing optional row to V-12 in both its heading AND any cross-references (search the file for "V-11" before editing to confirm there are no other references). The new V-11 row MUST follow the same row format used by V-1 through V-10: a `### V-11 — <title>` heading, then bullet rows for `**Behavior:**`, `**Command:**`, `**Pass criteria:**`, `**Gate:**`. For the new V-11: Behavior text should say that surfaces declared as `bg-slate-50` inside a `bg-surface-warm` parent MUST render a different computed color than that parent — this is the inverse of V-10's continuity assertion and is the load-bearing test that would have caught BL-01. Command text should specify `python3 -m http.server 8080` then load `http://localhost:8080/` and complete the quiz to reach the results page, then open DevTools console. Pass criteria should specify the concrete sample point — the first 3-up grid card at line 671 of `index.html`, reachable via a `document.querySelector` targeting the first `.bg-slate-50` element inside the reference-matrix `<section>` (the executor MUST encode the selector as `document.querySelector('section.bg-surface-warm .grid .bg-slate-50')` or an equivalent specific path; verify the selector returns the expected element before encoding) — and assert that its `getComputedStyle(...).backgroundColor` is NOT equal to the computed background color of its closest `bg-surface-warm` ancestor (use `card.closest('.bg-surface-warm')` to obtain the parent). The pass condition is strict inequality between the two `backgroundColor` strings. Gate text should say "After every commit that touches `--neutral-50-rgb` OR `--surface-rgb` OR the Tailwind `slate.50` / `surface.warm` config entries, AND as part of every Phase-end full sweep going forward." Then, update the "Per-Task Verification Map" table near the top of the file: add a new row mapping "Block 9: BL-01 closure (Phase 2 gap-closure plan 02-06)" to "Plan Wave 6" / Requirement "REQ-overdrive-default-theme" / Test Type "DevTools" / Verification Command "V-11 (new)" / Status "⬜ pending". Also update the "Manual-Only Verifications" table to add a row for V-11: Behavior "Surface-differentiation guard (`bg-slate-50` vs `bg-surface-warm`)", Requirement "REQ-overdrive-default-theme + BL-01 regression prevention", Why Manual "DevTools `getComputedStyle` assertions require live browser runtime", Test Instructions "See V-11 above. After every `--neutral-50-rgb` or `--surface-rgb` change AND Phase-end." Do NOT touch V-1 through V-10. Do NOT touch the V-1 Orchestrator Run Log at the bottom. Do NOT change `nyquist_compliant` or `status` frontmatter fields (the orchestrator handles those after this plan ships). Save the file.
  </action>
  <verify>
    <automated>grep -cE '^### V-11 — Surface-differentiation guard' .planning/phases/02-overdrive-default-theme-migration/02-VALIDATION.md</automated>
    <!-- Expected: 1 (the new V-11 block exists) -->
    <automated>grep -cE '^### V-12 \(optional\) — `VM338:1' .planning/phases/02-overdrive-default-theme-migration/02-VALIDATION.md</automated>
    <!-- Expected: 1 (the old V-11 optional row was renumbered to V-12) -->
    <automated>grep -cE 'bg-slate-50.*bg-surface-warm|bg-surface-warm.*bg-slate-50' .planning/phases/02-overdrive-default-theme-migration/02-VALIDATION.md</automated>
    <!-- Expected: ≥1 (the new rig mentions both utilities) -->
    <automated>grep -cE 'closest\(.bg-surface-warm.\)|querySelector.*bg-slate-50' .planning/phases/02-overdrive-default-theme-migration/02-VALIDATION.md</automated>
    <!-- Expected: ≥1 (the rig encodes a concrete selector path) -->
    <automated>grep -c '^| Block 9' .planning/phases/02-overdrive-default-theme-migration/02-VALIDATION.md</automated>
    <!-- Expected: 1 (Per-Task Verification Map has new Block 9 row) -->
    <automated>grep -c 'V-11 (new)\|V-11 (Surface-differentiation' .planning/phases/02-overdrive-default-theme-migration/02-VALIDATION.md</automated>
    <!-- Expected: ≥1 (cross-references updated) -->
  </verify>
  <acceptance_criteria>
    - A new `### V-11 — Surface-differentiation guard for bg-slate-50 inside bg-surface-warm` section exists in 02-VALIDATION.md, slotted between the existing V-10 block and the prior-optional V-11 (now V-12) block.
    - The previously-optional V-11 row (Tailwind CDN noise diagnosis) is renumbered to V-12 in both its heading and any cross-references; no remaining stale `V-11 (optional)` reference points at the old slot.
    - The new V-11 block contains all four standard sub-rows: `**Behavior:**`, `**Command:**`, `**Pass criteria:**`, `**Gate:**` — in the same format used by V-1 through V-10.
    - The Pass criteria sub-row names a concrete sample point (first 3-up grid card around line 671 of index.html), encodes a concrete `querySelector` selector path, and asserts strict inequality between the consumer's `getComputedStyle(...).backgroundColor` and its closest `bg-surface-warm` ancestor's `getComputedStyle(...).backgroundColor`.
    - The "Per-Task Verification Map" table near the top of the file has a new row for "Block 9: BL-01 closure" mapped to "Plan Wave 6" and "V-11 (new)" with status "⬜ pending".
    - The "Manual-Only Verifications" table near the bottom has a new V-11 row using the same format as other manual rows.
    - No edits to V-1 through V-10 content; no edits to the V-1 Orchestrator Run Log; no edits to the frontmatter `nyquist_compliant` or `status` fields.
    - `git diff --stat .planning/phases/02-overdrive-default-theme-migration/02-VALIDATION.md` shows additions concentrated in the V-10/V-11/V-12 transition region plus the two table updates (Per-Task Verification Map + Manual-Only Verifications). No churn elsewhere.
  </acceptance_criteria>
  <done>
    02-VALIDATION.md now contains a V-11 rig that, if it had existed before Phase 2 verification, would have caught BL-01 by asserting the computed-color delta between a `bg-slate-50` consumer and its `bg-surface-warm` parent. The optional Tailwind CDN noise row is preserved at V-12. The Per-Task Verification Map and Manual-Only Verifications tables both reflect the new rig. The next verification round (after this gap-closure plan ships) will have a load-bearing test for this regression class.
  </done>
</task>

</tasks>

<verification>
**Source-level proof BL-01 is closed (machine-verifiable, no browser required):**

```bash
# 1. Surface anchor unchanged
grep -nE -- '--surface-rgb:[[:space:]]+255 248 240' index.html
# Expected: line 55 exactly one match

# 2. Neutral-50 re-anchored to new value
grep -nE -- '--neutral-50-rgb:[[:space:]]+250 243 233' index.html
# Expected: line 66 exactly one match

# 3. Old neutral-50 value fully retired (no stragglers)
grep -nE -- '--neutral-50-rgb:[[:space:]]+255 248 240' index.html
# Expected: zero matches

# 4. New comment landed (future-reader protection per empirical-probes failure mode)
grep -nE -- '#FAF3E9 one step warmer-deeper than --surface-rgb' index.html
# Expected: line 66 exactly one match

# 5. Token contract structure preserved (D-07 ramp completeness; no token retirement)
grep -cE '^[[:space:]]*--neutral-[0-9]+-rgb:' index.html
# Expected: 10

# 6. Tailwind config slate.50 mapping unchanged (no Tailwind config edits)
grep -cE 'slate-50|var\(--neutral-50-rgb\)' index.html
# Expected: ≥2 (at least the Tailwind config entry on ~line 109 and the markup consumers)

# 7. Markup unchanged — no consumer site retouched
grep -cE 'bg-slate-50' index.html
# Expected: 8 (the 7 affected sites — 4 <th> at 615-618 + 3 cards at 671/680/689 — plus the recommendation panel at line 548)
```

**Runtime proof (DevTools, after `python3 -m http.server 8080`, load `http://localhost:8080/`, complete the quiz to reach the results page, scroll to the reference matrix section):**

1. `getComputedStyle(document.documentElement).getPropertyValue('--neutral-50-rgb').trim()` returns `'250 243 233'`.
2. The new V-11 rig assertion: `getComputedStyle(document.querySelector('section.bg-surface-warm .grid .bg-slate-50')).backgroundColor !== getComputedStyle(document.querySelector('section.bg-surface-warm .grid .bg-slate-50').closest('.bg-surface-warm')).backgroundColor` returns `true` (specifically: card returns `rgb(250, 243, 233)`, parent section returns `rgb(255, 248, 240)`).
3. Visual sanity (no assertion, just eye-check by the executor): the 3 grid cards at lines 671/680/689 have visible fill against the parent section; the 4 `<th>` headers at lines 615-618 have visible fill against the surrounding table area.
4. Side-effect check at line 548 (the `bg-slate-50` recommendation panel inside `bg-surface-elev` white parent): the panel reads as a subtle warm tint. Per the planning_context this is expected and acceptable — flag to the user only if it appears jarring against the white parent card.

**Validation-strategy proof:**

- 02-VALIDATION.md V-11 (new) exists with concrete `getComputedStyle` assertion encoded.
- 02-VALIDATION.md V-12 (formerly optional V-11 Tailwind CDN noise) is renumbered with no stale `V-11 (optional)` references remaining.
- Per-Task Verification Map has a new Block 9 row pointing to Plan Wave 6.
- Manual-Only Verifications table has a new V-11 row.

**Regression-prevention proof (no scope creep):**

- `git diff --stat index.html` shows the change confined to a single line in the `:root` block (the `<head>` region between lines 48-85). No `<style>` block touched. No Tailwind config touched. No markup touched.
- WR-01..06 from 02-REVIEW.md are NOT addressed in this plan — `grep` for `.answer-card { background: white;` and `.check-card { background: white;` still returns the pre-existing literals; `--neutral-400-rgb`, `--neutral-500-rgb`, `--border-rgb` values are unchanged; the sticky-footer gradients at lines 481/517 are unchanged; the override-notice icon at lines 579-582 is unchanged. Per user's locked scope decision, those remain deferred to a future phase / plan.
</verification>

<success_criteria>
**Plan-level success criteria (BL-01 closure):**

1. `--neutral-50-rgb` and `--surface-rgb` carry distinct RGB triplets in the `:root` block of `index.html` — `--surface-rgb` remains `255 248 240`, `--neutral-50-rgb` becomes `250 243 233`.
2. The reference-matrix `<th>` headers (lines 615-618) and 3-up grid cards (lines 671/680/689) render with a fill color computationally distinct from their `bg-surface-warm` parent `<section>` (verified at the source level by the now-distinct triplets; verified at runtime by the new V-11 rig).
3. Line 66 of `index.html` carries an inline comment articulating the deliberate distinction (`/* #FAF3E9 one step warmer-deeper than --surface-rgb; bg-slate-50 consumers must differentiate from bg-surface-warm parents */`).
4. 02-VALIDATION.md contains a new V-11 rig encoding a `getComputedStyle` delta assertion between a `bg-slate-50` consumer and its `bg-surface-warm` parent. The previously-optional V-11 row is renumbered to V-12.
5. No regression on V-3, V-4, V-5, V-9, or V-10: no JS changes; no token retirements; no markup churn; `--surface-rgb` unchanged; Tailwind config unchanged; Google Fonts link unchanged; component `<style>` block unchanged.

**Phase-level downstream success criteria (when 02-VERIFICATION.md is re-run after this plan ships):**

6. Phase 2 SC #1 ("Overdrive identity end-to-end across all six slides AND the results page") flips from PARTIAL-FAIL to VERIFIED.
7. Phase 2 SC #3 ("internally coherent") holds in both literal text AND spirit — cards-inside-section visual hierarchy is preserved, not collapsed.
8. BL-01 is closed; gaps list in 02-VERIFICATION.md (after re-run) is empty.
9. `STATE.md` `status` field can flip from `gaps_found` to `verified` once 02-VERIFICATION.md is re-run.

**Scope-boundary success criteria (no creep):**

10. WR-01 through WR-06 from 02-REVIEW.md remain unaddressed in this plan. They are explicitly out of scope per user's locked decision. Surfacing them as candidates for Phase 3 or a future gap-closure plan is acceptable in the executor's SUMMARY.md, but no implementation work on them is permitted in plan 02-06.
</success_criteria>

<output>
After completion, create `.planning/phases/02-overdrive-default-theme-migration/02-06-SUMMARY.md` documenting:

- The one-line value flip on index.html line 66 (old → new, with the comment update inline).
- The 02-VALIDATION.md V-11 rig addition (and V-12 renumbering of the prior optional row).
- Source-level grep evidence that BL-01 is closed.
- A note on the side-effect at line 548 (the `bg-slate-50` recommendation panel inside the `bg-surface-elev` white parent now reads as a subtle warm tint) — confirm whether this is acceptable per the planning_context note or requires human review.
- Explicit confirmation that WR-01..06 were NOT touched (list each by name and state "deferred per user's locked scope decision").
- A pointer to re-running 02-VERIFICATION.md so SC #1 can flip from PARTIAL-FAIL to VERIFIED.
- 2 atomic commits expected: one for the index.html token flip + comment, one for the 02-VALIDATION.md V-11 rig addition.
</output>
