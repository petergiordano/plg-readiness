---
context: phase
phase: 02-overdrive-default-theme-migration
task: null
total_tasks: null
status: in_progress
last_updated: 2026-05-16T13:16:25.452Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. The constraint below was discovered through actual Phase 1 failure (V-4).
> Acknowledge it explicitly before proceeding to `/gsd-plan-phase 2`.

- [ ] CONSTRAINT: **empirical-probes-on-head-element-ordering** — Grep-ordering checks are NOT sufficient for runtime-behavior claims about `<head>` element ordering. Phase 1 RESEARCH §5's "CDN is defer-equivalent" claim was structurally checked but never browser-loaded; V-4 caught the runtime `ReferenceError` after 3 commits had landed. **Structural mitigation (D-14 in 02-CONTEXT.md):** Phase 2 must browser-verify (`python3 -m http.server` + DevTools console — zero JS errors AND visible identity matches baseline) BEFORE the plan locks any proposed `<head>` ordering or `<script>`/`<link>` change, AND after every `<head>`-touching execute task. VALIDATION.md must encode as hard verify gate rows, not a single end-of-phase sweep.

**Do not proceed until this box is checked AND you can answer all three:**
1. What is this anti-pattern? (in your own words)
2. How did it manifest? (specific Phase 1 V-4 failure description)
3. What structural mechanism (not acknowledgment) prevents recurrence? (Name the concrete browser-verify gates in research-phase and execute-phase.)

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| empirical-probes-on-head-element-ordering | Phase 1 V-4: grep-ordering assertions passed; the runtime claim ("CDN is defer-equivalent") was never browser-loaded; ReferenceError shipped to 3 commits before final visual sweep caught it. | blocking | D-14 (02-CONTEXT.md): research-phase browser-verify BEFORE PLAN.md locks; execute-phase browser-verify after every `<head>`-touching task; encoded as hard VALIDATION.md rows. Grep ordering necessary but NOT sufficient. |
| line-range-extrapolation-in-planning-artifacts | This session (caught pre-commit): wrote 3 wrong line ranges into 02-CONTEXT.md by extrapolating endpoints from earlier grep results that only showed range starts. CONTEXT.md becomes downstream agents' ground truth. | advisory | Re-grep BOTH start AND end of every cited line range at write-time. Never extrapolate endpoints from neighboring sections. Lesson in ~/.claude/lessons.md (auto-loaded at SessionStart). |
| stale-codebase-maps-2026-03-05 | `.planning/codebase/` maps dated 2026-03-05 reference pre-Phase-1 state (mention `primary`/`primaryHover` + Inter; describe pre-Phase-1 Tailwind config + file layout). | advisory | Phase 2 researcher/planner re-grep `index.html` directly for tokens, line anchors, current Tailwind config shape. Treat `.planning/codebase/` as historical context only. Re-running `/gsd-map-codebase` would refresh for Phase 3 but is out of scope for Phase 2. |
| claude-code-worktree-spawn-base-quirk | Phase 1 executor's spawned worktree forked off main (04a9b0f), not orchestrator's HEAD (rebrand-theming @ e3a92fd). Executor self-corrected pre-commit; safe because branch had zero unique commits. | advisory | Executor agents assert worktree's expected base in their FIRST action and HALT if drift detected post-commit (do NOT self-recover post-commit — surface to user for manual reconciliation). |

<current_state>
Phase 2 discuss complete. `02-CONTEXT.md` (14 decisions, D-01..D-14) + `02-DISCUSSION-LOG.md` committed at d21fcd1. STATE.md updated and committed at b2dd7e1 (which also retired the prior between-phases HANDOFF.json — superseded by this new one). Branch `rebrand-theming` ahead 3 vs `origin/rebrand-theming`. Working tree clean except pre-existing untracked `.planning/COACH-STATE.md` (not session-related).

Next action: `/gsd-plan-phase 2` — research → pattern-map → planner → plan-checker loop. The planner MUST consume `02-CONTEXT.md` and encode D-14's browser-verify gates as hard VALIDATION.md rows.
</current_state>

<completed_work>

This session:
- Ran `/gsd-resume-work` from previous Phase-1-complete paused state. Acknowledged HANDOFF.json's blocking constraint (empirical-probes-on-head-element-ordering) with three-question understanding check before proceeding.
- Ran `/gsd-discuss-phase 2` end-to-end. 4 gray areas selected and discussed (dark hero replacement & orange-as-structure; warm-neutral ramp; Cat D + hover-state literals; display-font swap depth). 14 decisions captured (D-01..D-14).
- Caught and corrected 3 off-by-one line-range errors in 02-CONTEXT.md BEFORE commit (token contract 13–84 not 13–83; CDN at line 85 not 84; Tailwind config 86–123 not 85–119). Captured lesson in `~/.claude/lessons.md`.
- Wrote `02-CONTEXT.md` (~25KB, 14 decisions + canonical refs + code context + specifics + deferred).
- Wrote `02-DISCUSSION-LOG.md` (~12KB, full audit trail of all 14 questions with options and selections).
- Deleted `02-DISCUSS-CHECKPOINT.json` (one-shot, folded into CONTEXT.md).
- Updated STATE.md (current focus, position, todos, session continuity, read-only references).
- Deleted prior HANDOFF.json (one-shot artifact from `/gsd-pause-work` between Phase 1 and 2; carry-over folded into 02-CONTEXT.md D-14 + canonical_refs).
- Wrote new HANDOFF.json (this pause).

Commits landed on `rebrand-theming`:
- `d21fcd1` — `docs(02): capture phase context`
- `75f8836` — `docs(state): record phase 2 context session` (SDK timestamp bump only)
- `b2dd7e1` — `docs(state): Phase 2 context gathered; retire one-shot HANDOFF.json`

</completed_work>

<remaining_work>

For Phase 2:
- `/gsd-plan-phase 2` — produces `02-RESEARCH.md`, `02-PATTERNS.md`, `02-VALIDATION.md`, `02-NN-PLAN.md`. Per D-14, research-phase MUST browser-verify any proposed `<head>` ordering or `<script>`/`<link>` change BEFORE PLAN.md locks.
- `/gsd-execute-phase 2` — once plan-checker passes.
- `/gsd-verify-phase 2` — after execution; D-14 strict browser-verify gates apply.

For the milestone (after Phase 2 ships):
- Phase 3: Second Theme Stub & Pluggability Proof.
- Re-run `/how-it-works` (HOW_IT_WORKS.md flagged stale at SessionStart).
- Optionally re-run `/gsd-map-codebase` to refresh `.planning/codebase/` for Phase 3.

</remaining_work>

<decisions_made>

All 14 in `02-CONTEXT.md` `<decisions>`. Summary by category:

**Surface palette — dark hero retirement (D-01..D-05):**
- D-01: Continuous warm off-white (#FFF8F0) across slides + results + footer. `--surface-dark-rgb` and `--surface-dark-card-rgb` retire.
- D-02: Main result card on white (#FFFFFF) elevated surface with 3px #FF9000 left-border. Wires `--surface-elev-rgb`.
- D-03: Wedge + override callouts on white card with 4–6px #FF9000 top-strip.
- D-04: Footer warm bg + #FF9000 top rule.
- D-05: Three orange section dividers on results page (above main card, line 601 boundary, above footer).

**Neutral ramp (D-06..D-08):**
- D-06: Warm-gray 10-stop ramp. Planner derives from Overdrive anchors (#FFF8F0, #E5E5E5, #8A8A8A, #666666, #434343).
- D-07: Keep full 10 stops declared (50–900) even where 700/800/900 have no current consumer.
- D-08: Keep `slate-*` utility name in markup (no rename to `neutral-*`).

**Secondary palette + hovers (D-09..D-12):**
- D-09: Cat D indigo utilities → Light Yellow #FFE599 + Golden Yellow #F1C232 per role.
- D-10: Warning icon (line 575) → Golden Yellow #F1C232 (tokenized).
- D-11: Hovers (lines 177/190/210) → `rgb(var(--accent-rgb) / 0.4)` border + `rgb(var(--accent-rgb) / 0.06)` bg. Zero new hover tokens.
- D-12: +2 net-new tokens (`--brand-soft-rgb` = #FFE599, `--brand-secondary-rgb` = #F1C232). Tailwind config remaps yellow.100/yellow.400.

**Typography (D-13):**
- D-13: Family swap Fraunces → Space Grotesk + minimum-viable adjustments (browser-decided).

**Verification methodology (D-14):**
- D-14: Browser-verify BEFORE plan locks AND after every `<head>`-touching execute task. Structural mitigation for V-4 lesson; phase-level standard.

</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/phases/02-overdrive-default-theme-migration/02-CONTEXT.md` — canonical input for `/gsd-plan-phase 2`. Decisions D-01..D-14, canonical refs, code context, specifics, deferred.
2. `.planning/HANDOFF.json` — structured carry-over including `blocking_constraints_for_phase_2[0]` (must demonstrate understanding before proceeding).
3. `.planning/phases/01-theming-architecture-foundation/01-01-SUMMARY.md` § "Hand-off to Phase 2" — Category B/C/D hex catalogs with post-Phase-1 line numbers (re-grep at start per advisory; line numbers verified current as of 2026-05-16).
4. `.planning/phases/01-theming-architecture-foundation/01-CONTEXT.md` — Phase 1 D-01..D-16 (carry-forward decisions that Phase 2 inherits).
5. `.planning/phases/01-theming-architecture-foundation/01-RESEARCH.md` § "Pitfall 2b" — the V-4 lesson source.
6. `docs/design/design-system-alpha-overdrive.md` — Overdrive design system. Phase 2 critical sections: §3 Color Palette, §4 Typography, §5 Layout, §9 Brand Personality / Signature Move.
7. `index.html` (1002 lines) — grep current state; do NOT trust `.planning/codebase/` maps (dated 2026-03-05, pre-Phase-1).

## Infrastructure State
- **Branch:** `rebrand-theming` (worktree at `/Users/petergiordano/Documents/GitHub/plg-readiness-rebrand`); main worktree at `/Users/petergiordano/Documents/GitHub/plg-readiness` is on `main`.
- **Remote:** `origin/rebrand-theming` (ahead 3 commits: d21fcd1, 75f8836, b2dd7e1). Push when convenient.
- **No running services / background processes.**
- **Working tree:** clean except pre-existing untracked `.planning/COACH-STATE.md` (not session-related).
- **HOW_IT_WORKS.md:** flagged stale at SessionStart (structural drift detected: removed dirs `reference`; removed entry points `index.html`; removed tech `tailwind`; deploy method changed from 'manual' to 'null'). Regenerate via `/how-it-works` after Phase 2 ships.

<context>
Phase 2 takes the structural theming contract built in Phase 1 (D-08 zero-visual-diff guarantee held current values) and flips the **values** in `:root` to deliver Overdrive end-to-end. The contract structure does not change — only token values change, plus 2 net-new tokens (D-12), plus one rewired token consumer (`--surface-elev-rgb` declared in Phase 1, now consumed by the elevated result card per D-02).

Net contract growth: +2 tokens (~13 → ~15 color tokens). Retires --surface-dark-rgb + --surface-dark-card-rgb. The "no frankenstein" rule (REQ-overdrive-default-theme acceptance #3) is the load-bearing constraint — every surface, control, badge, card, divider, and font must coherently render Overdrive end-to-end.

The V-4 lesson is structurally encoded in D-14 and explicit in HANDOFF.json's `blocking_constraints_for_phase_2[0]`. Research and planner must run the browser, not just grep. Phase 2 touches `<head>` (Google Fonts `<link>` swap for Space Grotesk) — same risk surface as Phase 1's V-4 bug.
</context>

<next_action>
Run `/clear` for a fresh planning context, then `/gsd-plan-phase 2`.

The planner pipeline (research → pattern-map → planner → plan-checker) will read 02-CONTEXT.md as its canonical input. Watch for:
- The research agent producing a proposed `<head>` ordering. Before that ordering is written into PLAN.md, browser-verify it (`python3 -m http.server` + DevTools).
- The planner encoding D-14's browser-verify gates as hard VALIDATION.md rows (not deferring to a single end-of-phase sweep).
- The plan-checker passing all rows (Phase 1 took 2 iterations on plan-checker — expect similar).
</next_action>
