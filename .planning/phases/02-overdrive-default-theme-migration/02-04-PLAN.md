---
phase: 02-overdrive-default-theme-migration
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - index.html
autonomous: true
requirements:
  - REQ-overdrive-default-theme
user_setup: []

must_haves:
  truths:
    - "The dark hero region (currently `bg-surface-dark` wrapper at line 533) is replaced with a warm-surface `<section>` carrying the D-05(a) orange top-rule divider above the main result card."
    - "The main result card (line 538) sits on `bg-surface-elev` (white) with a 3px orange left-border per D-02 — the inline `style=\"border-left: 3px solid rgb(var(--accent-rgb));\"` is the structural accent."
    - "The wedge callout (line 552) and override-notice callout (line 572) each carry a 5px orange top-strip on a white card body per D-03 — no dark surfaces remain on either callout."
    - "The override-notice warning icon (line 575) renders Golden Yellow (#F1C232) via `text-yellow-400` (resolving through `--brand-secondary-rgb` per D-12 + D-10)."
    - "The reference matrix `<section>` (line 597) carries the D-05(b) orange top-rule divider; bg is `bg-surface-warm` per D-01 continuous-surface rule."
    - "The PLS badge (line 635) renders `bg-yellow-100 text-ink` (per R-2 contrast override) — Light Yellow bg + Dark Gray text, WCAG AA compliant."
    - "The PLS card icon bg (line 676) renders `bg-yellow-100` Light Yellow per D-09."
    - "The footer (line 698) sits on `bg-surface-warm` with a 4px orange top-rule per D-04 — this rule doubles as the D-05(c) divider."
    - "V-2b (post-Block-3) computed-style check now passes for `#result-container` having `bg-surface-elev` resolving to white."
    - "V-4 grep returns 0 matches for dark-surface utilities or hex literals."
    - "V-10 passes: warm off-white surface continuous across body, results page, reference matrix wrapper, footer."
  artifacts:
    - path: "index.html"
      provides: "Results page region (lines 530-704) rewritten end-to-end: dark hero retired; orange section dividers above main card + above reference matrix + above footer; main result card on white with orange left-border; callouts on white with orange top-strips; warning icon Golden Yellow; PLS badge + PLS card icon Light Yellow + Dark Gray text."
      contains: "border-left: 3px solid rgb(var(--accent-rgb))"
      contains_also: "bg-surface-elev"
      contains_also_2: "bg-yellow-100"
      contains_also_3: "border-t-[4px] border-accent"
  key_links:
    - from: "Main result card (line 538)"
      to: "--accent-rgb (token contract) + --surface-elev-rgb (token contract)"
      via: "inline style + bg-surface-elev utility (added in Plan 01 Task 2)"
      pattern: "bg-surface-elev.*border-left.*var\\(--accent-rgb\\)"
    - from: "PLS badge + PLS card icon (lines 635, 676)"
      to: "--brand-soft-rgb (Light Yellow token)"
      via: "bg-yellow-100 utility (added in Plan 01 Task 2)"
      pattern: "bg-yellow-100"
    - from: "Override-notice warning icon (line 575)"
      to: "--brand-secondary-rgb (Golden Yellow token)"
      via: "text-yellow-400 utility (added in Plan 01 Task 2)"
      pattern: "text-yellow-400"
---

<objective>
Retire the dark results hero and migrate the entire results page region (lines 530-704) to Overdrive identity: warm off-white surfaces, white elevated cards, orange structural dividers (above main card + above reference matrix + above footer), orange top-strips on the two conditional callouts (D-03), orange left-border on the main result card (D-02), Light Yellow / Golden Yellow secondary palette on the PLS badge + PLS card icon bg + warning icon (D-09 + D-10), warm-surface footer with orange top-rule (D-04). This is the largest concentrated diff in Phase 2 and the most visible identity surface — without this plan, the user reaches the results page after a coherently-Overdrive quiz flow only to see the OLD dark hero + indigo callouts, which is the worst possible failure of acceptance criterion #3.

Purpose: D-01 retires dark backgrounds. D-02/D-03/D-04/D-05 establish orange-as-structure (the Overdrive design system §9 signature). This plan delivers all of them.

Output: One commit. ~30 line edits inside the results page region. Does NOT touch `<head>` content (body markup only) → does NOT fire D-14; DOES require V-3 visual coherence + V-4 dark-surface grep + V-10 continuous-surface verify.

**Resolved planner decisions:**
- Q-5 (D-05(a) section divider element shape): replace line 533's `<div class="bg-surface-dark pt-20 pb-16 md:pb-24">` with `<section class="bg-surface-warm pt-20 pb-16 md:pb-24 border-t-[4px] border-accent">`. The `<section>` element conveys semantic boundary; `border-t-[4px] border-accent` carries the orange rule in one declaration. Per Q-5 recommendation.
- D-05 divider thickness: 4px (`border-t-[4px]`) across all three section dividers (above main card, above reference matrix, above footer). Consistency over per-divider tuning.
- D-03 callout top-strip thickness: 5px (`border-top: 5px solid rgb(var(--accent-rgb));` via inline style). Mid-range of the D-03 4-6px discussion. Differentiates from the 4px section dividers; callout strips read as MORE prominent because they are smaller surface area / higher per-pixel impact.
- D-03 callout treatment: inline `style="border-top: 5px solid rgb(var(--accent-rgb));"` on a `bg-surface-elev` wrapper. Inline style for the brand accent strip (Pattern P-4) is acceptable per PATTERNS.md P-4 rule 1 ("acceptable for structural brand-element accents when the corresponding Tailwind utility would be more verbose").
- R-2 (PLS badge contrast): override D-09 — use `bg-yellow-100 text-ink` (NOT `bg-yellow-100 text-yellow-400`). `#434343` Dark Gray on `#FFE599` Light Yellow ≈ 9.1:1, passes WCAG AA. The yellow-tier secondary signal is carried by the bg color (Light Yellow distinguishes PLS from PLG-orange and SLG-neutral); fg color is text readability. Per planning_context R-2 recommendation.
- Wedge inner recommendation card (line 543, currently dark-on-dark `style="background:rgba(15,23,42,0.5);"` on `border-slate-700`): migrate per PATTERNS.md P-4 line-543 recommendation to `class="rounded-lg p-5 border border-slate-200 bg-slate-50"` — drop inline style entirely; the bg comes from the re-themed `bg-slate-50` utility (post-Phase-2 resolves to warm-50 via `--neutral-50-rgb` = `255 248 240` ≈ warm off-white).
- D-10 warning icon bg (line 574 currently `style="background:rgba(245,158,11,0.15);"`): keep inline style; migrate to `style="background: rgb(var(--brand-secondary-rgb) / 0.15);"` per PATTERNS.md P-4 line-574 recommendation. Keeps the 0.15-alpha visual weight; retones to golden-yellow.
- D-05(c) divider above footer: NOT a separate element. The footer's own `border-t-[4px] border-accent` (D-04 treatment) IS the D-05(c) divider. One element does both jobs — minimum markup.
- Line 538 inline-style (the LAST `#4F46E5` literal in the file after Plan 03): migrate to `style="border-left: 3px solid rgb(var(--accent-rgb));"` per PATTERNS.md P-4 line-538.

**Threat-model coverage:** T-2-05 (Cat D markup utility swap + Cat C inline-style migration) — N/A; no behavior change. See `<threat_model>` below.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-overdrive-default-theme-migration/02-CONTEXT.md
@.planning/phases/02-overdrive-default-theme-migration/02-RESEARCH.md
@.planning/phases/02-overdrive-default-theme-migration/02-PATTERNS.md
@.planning/phases/02-overdrive-default-theme-migration/02-VALIDATION.md
@.planning/phases/02-overdrive-default-theme-migration/02-01-PLAN.md
@.planning/phases/02-overdrive-default-theme-migration/02-02-PLAN.md
@.planning/phases/02-overdrive-default-theme-migration/02-03-PLAN.md
@index.html

<interfaces>
<!-- Results page region (lines 530-704) — re-grep before editing. -->
<!-- All line anchors below were verified at 2026-05-16 HEAD b1af5f7. -->

Anatomy of the results page (read top-to-bottom):
- Line 533:  `<div class="bg-surface-dark pt-20 pb-16 md:pb-24">` — dark hero wrapper to retire (D-01 + D-05a)
- Line 534:  `<div class="max-w-[680px] ...">` — content container, KEEP unchanged
- Line 538:  Main result card with dark-card bg + indigo inline-style left-border (D-02 target)
- Line 541:  `<h2 id="result-title" class="font-display text-3xl font-bold text-slate-100 ..."` — text needs light->ink (D-01)
- Line 542:  `<p id="result-summary" class="... text-slate-400 ...">` — text needs slate-400 -> slate-500
- Line 543:  Inner recommendation card with dark-on-dark inline-style + dark border (Pattern P-4 line-543)
- Line 544:  `<h4 class="font-semibold text-slate-300 mb-2 text-sm">Recommendation</h4>` — text-slate-300 -> text-ink (NOTE: this element does NOT carry `font-display` or `text-lg`; preserve only `font-semibold mb-2 text-sm`)
- Line 545:  `<p id="result-recommendation" class="... text-slate-400">` — text-slate-400 -> text-slate-500
- Lines 552-569: Wedge callout (D-03 target — orange top-strip + white bg)
- Lines 572-584: Override-notice callout (D-03 + D-10 target — orange top-strip + golden-yellow warning icon)
- Line 589:  Restart button with broken-on-light hover (`hover:text-slate-300` lightens; needs flip to darken)
- Line 597:  `<section class="bg-white border-t border-slate-200 py-16 md:py-24">` — reference matrix wrapper (D-05b + D-01 target)
- Line 635:  PLS badge `bg-indigo-100 text-indigo-800` (D-09 + R-2 target)
- Line 676:  PLS card icon bg `bg-indigo-50` (D-09 target)
- Line 677:  PLS card icon stroke `text-accent` — DEFERRED IDEA in CONTEXT (stays text-accent per RESEARCH recommendation; orange icon on yellow bg)
- Line 698:  Footer `<footer class="bg-surface-dark border-t border-slate-800 py-10">` — D-04 + D-05c target
- Line 699:  Footer text `text-sm text-slate-500` — KEEP unchanged (re-themes through warm-gray ramp)

Cat C inline-style sites (Pattern P-4):
- Line 538:  `style="border-left: 3px solid #4F46E5;"`  -> `style="border-left: 3px solid rgb(var(--accent-rgb));"`
- Line 543:  `style="background:rgba(15,23,42,0.5);"`    -> DROP entirely; use Tailwind classes only
- Line 574:  `style="background:rgba(245,158,11,0.15);"` -> `style="background: rgb(var(--brand-secondary-rgb) / 0.15);"`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retire dark hero + migrate main result card + inner recommendation card (lines 533-548)</name>
  <files>index.html</files>
  <read_first>
    - index.html (re-grep `bg-surface-dark` and `bg-surface-dark-card` to confirm current line anchors)
    - .planning/phases/02-overdrive-default-theme-migration/02-CONTEXT.md (D-01, D-02, D-05)
    - .planning/phases/02-overdrive-default-theme-migration/02-RESEARCH.md (## File-Level Scope → Results page region → Dark hero retirement section)
    - .planning/phases/02-overdrive-default-theme-migration/02-PATTERNS.md (P-4 line-538 + line-543; P-5 lines 533/538/541/542/543/544/545)
    - .planning/phases/02-overdrive-default-theme-migration/02-VALIDATION.md (V-4, V-10)
    - .planning/phases/01-theming-architecture-foundation/01-01-SUMMARY.md (Cat C catalog row for line 538)
  </read_first>
  <action>
Edit lines 533-548 of `index.html` in sequence:

(a) Line 533 — replace the dark hero wrapper with the D-05(a) section divider per Q-5:
  - Current: opening tag `<div class="bg-surface-dark pt-20 pb-16 md:pb-24">`
  - Target:  opening tag `<section class="bg-surface-warm pt-20 pb-16 md:pb-24 border-t-[4px] border-accent">`
  - The closing tag for this element MUST also change from `</div>` to `</section>` — re-grep the matching close tag for this opening div before editing (likely around line 590-595 based on existing structure; verify by reading the file).

(b) Line 538 — main result card surface + border + Cat C inline-style migration (D-02):
  - Current class list: `bg-surface-dark-card rounded-xl border border-slate-700 overflow-hidden`
  - Target class list:  `bg-surface-elev rounded-xl border border-slate-200 overflow-hidden`
  - Current inline style: `style="border-left: 3px solid #4F46E5;"`
  - Target inline style:  `style="border-left: 3px solid rgb(var(--accent-rgb));"`

(c) Line 541 — result title text color (P-5 row for line 541):
  - In the class list of `<h2 id="result-title" ...>`, change `text-slate-100` → `text-ink`. Preserve `font-display text-3xl font-bold mb-4 leading-tight` and any other classes.

(d) Line 542 — result summary text color:
  - Change `text-slate-400` → `text-slate-500` (light-mode equivalent per PATTERNS.md P-5; matches line-603 pattern in reference matrix).

(e) Line 543 — inner recommendation card (Pattern P-4 line-543, the wedge inner card mentioned in CONTEXT deferred-idea-line-6):
  - Current: `<div class="rounded-lg p-5 border border-slate-700" style="background:rgba(15,23,42,0.5);">`
  - Target:  `<div class="rounded-lg p-5 border border-slate-200 bg-slate-50">`
  - Drop the inline `style="..."` attribute entirely.

(f) Line 544 — inner heading text color:
  - Change `text-slate-300` → `text-ink`. Preserve `font-semibold mb-2 text-sm`. (Element is an `<h4>` with no `font-display` / `text-lg` — re-grep line 544 before edit per `<read_first>` discipline.)

(g) Line 545 — inner recommendation text:
  - Change `text-slate-400` → `text-slate-500`.

After edits land, the main result card (lines 538-548) renders on white with a 3px orange left-border, sitting inside a warm-surface `<section>` with a 4px orange top-rule divider. The inner recommendation card (line 543) sits on warm-off-white (re-themed `bg-slate-50` resolves to `--neutral-50-rgb` = warm off-white).

Do NOT modify the contents of `#result-recommendation` (line 545 textContent) — that's populated by `calculateScore()` at line 938 and must remain unchanged (EXCL-scoring-or-flow-changes).

Commit message: `feat(02-04): retire dark hero + migrate main result card to white + orange left-border (D-01, D-02, D-05a)`.
  </action>
  <verify>
    <automated>grep -nE 'bg-surface-dark[^-]|bg-surface-dark-card' index.html | wc -l</automated>
    Expected: 0 matches in markup (Tailwind config entries were deleted in Plan 01 Task 2; markup references are deleted in this task).
    <automated_2>grep -nE 'bg-surface-elev' index.html | wc -l</automated_2>
    Expected: 1+ matches (main result card line 538 + potentially other consumers from Plan 03 line 192 / line 262 `.kbd-hint span` — wait, Plan 03 wired those at CSS-level via `rgb(var(--surface-elev-rgb))`, not via Tailwind utility. So just 1 match here.)
    <automated_3>grep -nE 'border-t-\[4px\] border-accent' index.html | wc -l</automated_3>
    Expected: 1 match after this task (Task 3 adds 2 more for reference matrix + footer).
    <automated_4>grep -nE '#4F46E5' index.html | wc -l</automated_4>
    Expected: 0 matches (the LAST `#4F46E5` was on line 538 inline-style; this task migrates it).
  </verify>
  <acceptance_criteria>
    - **Source-assertion (grep, V-4 alignment):** `grep -nE '#0F172A|#1E293B|bg-surface-dark|bg-surface-dark-card|bg-slate-(7|8|9)00' index.html` returns 0 matches in markup (Tasks 1 + 2 + 3 must collectively retire all dark-surface references; Task 1 retires the hero wrapper + main result card; Tasks 2 + 3 retire callout + footer dark surfaces).
    - **Source-assertion (grep, V-5 alignment, FINAL elimination):** `grep -nE '#4F46E5' index.html` returns 0 matches across the entire file. Plan 03 retired all component `<style>` indigo literals; Task 1 retires the LAST one on line 538 inline-style.
    - **Behavior-assertion (visual):** Walk the quiz to results (P1 path from V-9: individual/high/individual/4+/0-2 → "Pure PLG: Ideal Candidate"). Results page shows: warm off-white bg with a 4px orange top-rule above the main result card; main result card sits on white with a 3px orange left-border; inner recommendation card sits on warm-off-white with subtle warm border. Zero dark surfaces visible.
    - **Behavior-assertion (V-2b partial — main result card surface):** DevTools: `getComputedStyle(document.querySelector('#result-container')).backgroundColor === 'rgb(255, 255, 255)'`. (This was a deferred check from Plan 01 Task 2 V-2b — it could not pass until markup consumed `bg-surface-elev`; this task makes it pass.)
    - **Behavior-assertion (V-10 partial):** Body bg + section bg (the new `<section>` from line 533) both render `rgb(255, 248, 240)`. Continuous surface from body through to results page wrapper. (Full V-10 — including footer + reference matrix wrapper — requires Tasks 2 + 3.)
  </acceptance_criteria>
  <done>
The dark hero is gone. Main result card renders on white with a 3px orange left-border. Inner recommendation card renders on warm-off-white. The `<section>` wrapper carries the D-05(a) orange divider above the result card. Zero indigo literals remain anywhere in the file. V-2b deferred check passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate wedge callout + override-notice callout + warning icon + restart button (lines 552-589)</name>
  <files>index.html</files>
  <read_first>
    - index.html (re-grep `id="wedge-callout"` and `id="override-notice"` to confirm current line anchors)
    - .planning/phases/02-overdrive-default-theme-migration/02-CONTEXT.md (D-03, D-10)
    - .planning/phases/02-overdrive-default-theme-migration/02-RESEARCH.md (## File-Level Scope → Wedge callout + Override-notice callout sections)
    - .planning/phases/02-overdrive-default-theme-migration/02-PATTERNS.md (P-4 line-574; P-5 rows for lines 552/560-565/572/575/580-581/589)
    - .planning/phases/02-overdrive-default-theme-migration/02-VALIDATION.md (V-3 sub-criterion e — Light Yellow + Golden Yellow on warning icon)
  </read_first>
  <action>
Edit lines 552-589 of `index.html`. Both callouts get the D-03 treatment (orange top-strip on white card body).

(a) Line 552 — wedge callout wrapper:
  - Current class list: `hidden result-reveal result-reveal-2 bg-slate-800/50 rounded-xl border border-slate-700 p-6 mb-5`
  - Target class list:  `hidden result-reveal result-reveal-2 bg-surface-elev rounded-xl p-6 mb-5`
  - Drop `bg-slate-800/50` (dark surface) and `border border-slate-700` (dark border). The orange top-strip is added via inline style.
  - Add inline style: `style="border-top: 5px solid rgb(var(--accent-rgb));"` — per planner decision in `<objective>` (5px mid-range of D-03's 4-6px discussion).
  - Final tag form: `<div id="wedge-callout" class="hidden result-reveal result-reveal-2 bg-surface-elev rounded-xl p-6 mb-5" style="border-top: 5px solid rgb(var(--accent-rgb));">`

(b) Lines 560-565 — wedge text colors (P-5 row says light-mode equivalents):
  - For each line in this range that has `text-slate-200` → change to `text-ink`.
  - For each line with `text-slate-300` → change to `text-slate-600`.
  - For each line with `text-slate-400` → change to `text-slate-500`.
  - Re-grep each anchor before editing; if a line has multiple text-color utilities (unlikely) handle them as named above.

(c) Line 572 — override-notice callout wrapper:
  - Apply the SAME treatment as line 552. Re-grep to confirm the current class list (likely identical shape to wedge callout per RESEARCH "same treatment as line 552"). Drop dark surfaces; add `bg-surface-elev`; add inline `style="border-top: 5px solid rgb(var(--accent-rgb));"`.

(d) Line 574 — warning icon container (Pattern P-4 line-574):
  - Current inline style: `style="background:rgba(245,158,11,0.15);"`
  - Target inline style:  `style="background: rgb(var(--brand-secondary-rgb) / 0.15);"`
  - Per planner decision in `<objective>`: keep inline; retones to golden-yellow at 0.15 alpha to preserve current visual weight.

(e) Line 575 — warning icon SVG color (D-10):
  - Change `text-amber-400` → `text-yellow-400`. (Post-Plan-01 Tailwind config, `text-yellow-400` resolves through `--brand-secondary-rgb` = `#F1C232` Golden Yellow.)

(f) Lines 580-581 — override-notice text colors:
  - `text-slate-200` → `text-ink`.
  - `text-slate-400` → `text-slate-500`.

(g) Line 589 — restart button hover fix:
  - Current: `class="text-sm text-slate-500 hover:text-slate-300 ..."`
  - Target:  `class="text-sm text-slate-500 hover:text-slate-700 ..."`
  - On a light bg, `hover:text-slate-300` LIGHTENS the text on hover (disappears). `hover:text-slate-700` DARKENS it (correct interaction feedback).

Commit message: `feat(02-04): migrate wedge + override callouts to orange-top-strip on white + golden-yellow warning icon (D-03, D-10)`.
  </action>
  <verify>
    <automated>grep -nE 'bg-slate-800/50|border-slate-700|border-slate-800' index.html | wc -l</automated>
    Expected: 0 matches (both callouts retired dark surfaces + dark borders; footer's `border-slate-800` migrated by Task 3).
    <automated_2>grep -nE 'text-amber-400' index.html | wc -l</automated_2>
    Expected: 0 matches (replaced by text-yellow-400).
    <automated_3>grep -nE 'border-top: 5px solid rgb\(var\(--accent-rgb\)\)' index.html | wc -l</automated_3>
    Expected: 2 matches (wedge callout + override callout top-strips).
    <automated_4>grep -nE 'background: rgb\(var\(--brand-secondary-rgb\) / 0\.15\)' index.html | wc -l</automated_4>
    Expected: 1 match (warning icon bg).
    <automated_5>grep -nE 'hover:text-slate-300' index.html | wc -l</automated_5>
    Expected: 0 matches (restart button hover fixed).
  </verify>
  <acceptance_criteria>
    - **Source-assertion (grep, positive):** All five `<automated>` greps return expected counts.
    - **Source-assertion (V-3 sub-criterion (e) — Light Yellow + Golden Yellow):** `grep -nE 'text-yellow-400' index.html` returns 1+ matches (warning icon).
    - **Behavior-assertion (visual — wedge callout):** Walk to a result that triggers the wedge callout (P4 path: csuite/any/any/4+/0-2 → "Sales-Led with Wedge Opportunity" with wedge-callout visible). Wedge callout shows: white card body, 5px solid orange top-strip, dark-gray heading + body text, NO dark surfaces, NO indigo accents.
    - **Behavior-assertion (visual — override notice):** Walk to a result that triggers the override notice (P5 path: csuite/any/any/0-3/3+ → "Sales-Led Growth Required" with override-notice visible). Override notice shows: white card body, 5px orange top-strip, warning icon container with soft golden-yellow tinted bg, warning SVG renders Golden Yellow (#F1C232).
    - **Behavior-assertion (restart button):** Hover the restart button; text color DARKENS (legibility increases on hover), not lightens.
    - **V-5 partial:** `grep -nE 'amber|#F59E0B' index.html` returns 0 matches (amber retired).
  </acceptance_criteria>
  <done>
Both callouts render on white with 5px orange top-strips. Warning icon renders Golden Yellow. Restart button hover is light-mode appropriate. No dark surfaces in callouts. No amber references remain.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate reference matrix section + PLS badge + PLS card icon bg + footer (lines 597-700)</name>
  <files>index.html</files>
  <read_first>
    - index.html (re-grep `bg-white border-t border-slate-200`, `bg-indigo-100`, `bg-indigo-50`, `bg-surface-dark`, `border-slate-800` to confirm current line anchors)
    - .planning/phases/02-overdrive-default-theme-migration/02-CONTEXT.md (D-04, D-05b, D-05c, D-09)
    - .planning/phases/02-overdrive-default-theme-migration/02-RESEARCH.md (## File-Level Scope → Reference Matrix section divider, Reference matrix table styling, Footer)
    - .planning/phases/02-overdrive-default-theme-migration/02-PATTERNS.md (P-5 rows for lines 597, 635, 676, 698)
    - .planning/phases/02-overdrive-default-theme-migration/02-VALIDATION.md (V-3, V-4, V-5, V-8, V-10)
  </read_first>
  <action>
Edit lines 597-700 of `index.html`:

(a) Line 597 — reference matrix `<section>` (D-05b + D-01):
  - Current: `<section class="bg-white border-t border-slate-200 py-16 md:py-24">`
  - Target:  `<section class="bg-surface-warm border-t-[4px] border-accent py-16 md:py-24">`
  - Per planner decision (`<objective>`): 4px orange top-rule for the D-05(b) divider; `bg-surface-warm` for D-01 continuous-surface coherence (the reference matrix was on `bg-white`; now it's on the same warm off-white as the rest of the page).

(b) Line 635 — PLS badge (D-09 + R-2 override):
  - Current: `<span class="... bg-indigo-100 text-indigo-800">Product-Led Sales</span>`
  - Target:  `<span class="... bg-yellow-100 text-ink">Product-Led Sales</span>`
  - Per R-2 planner decision: `text-ink` (Dark Gray) instead of `text-yellow-400` (Golden Yellow). Restores WCAG AA contrast (~9.1:1 vs failing ~1.3:1) while preserving the yellow-tier secondary-brand signal via bg color.
  - Preserve all other classes (likely `inline-block px-2 py-0.5 rounded text-xs font-medium` or similar; re-grep the full class attribute and substitute only the two indigo classes).

(c) Line 676 — PLS card icon bg (D-09):
  - Current: `<div class="w-9 h-9 bg-indigo-50 rounded-md ...">`
  - Target:  `<div class="w-9 h-9 bg-yellow-100 rounded-md ...">`
  - Substitute `bg-indigo-50` → `bg-yellow-100` only; preserve all other classes (`w-9 h-9 rounded-md flex items-center justify-center` or similar; re-grep the full class attribute).

(d) Line 677 — PLS card icon stroke: per RESEARCH recommendation, leave as `text-accent` (stays orange — orange icon on yellow bg gives the GTM-motion-hierarchy cue without losing structural orange). DO NOT EDIT. Document in commit.

(e) Line 698 — footer (D-04 + D-05c):
  - Current: `<footer class="bg-surface-dark border-t border-slate-800 py-10">`
  - Target:  `<footer class="bg-surface-warm border-t-[4px] border-accent py-10">`
  - Per D-04 + D-05c planner decision (`<objective>`): the footer's 4px orange top-rule IS the D-05(c) divider. No separate divider element needed. `bg-surface-warm` for D-01 continuous-surface.

(f) Line 699 — footer text:
  - VERIFY ONLY: `text-sm text-slate-500` should be UNCHANGED. Re-themes through warm-gray ramp automatically.

Commit message: `feat(02-04): migrate reference matrix + PLS badge + PLS card icon + footer to Overdrive secondary palette + warm surface + orange dividers (D-04, D-05bc, D-09)`.
  </action>
  <verify>
    <automated>grep -nE 'bg-indigo-(50|100)|text-indigo-800|bg-surface-dark[^-]?' index.html | wc -l</automated>
    Expected: 0 matches (all indigo + dark utilities retired across the entire file).
    <automated_2>grep -nE 'bg-yellow-100' index.html | wc -l</automated_2>
    Expected: 2 matches (PLS badge + PLS card icon bg).
    <automated_3>grep -nE 'border-t-\[4px\] border-accent' index.html | wc -l</automated_3>
    Expected: 3 matches across the file (Task 1 added section divider above main result card; this Task 3 adds reference matrix + footer).
    <automated_4>grep -nE 'bg-surface-warm' index.html | wc -l</automated_4>
    Expected: 4+ matches (body line 274 + new section at line 533 + reference matrix line 597 + footer line 698).
    <automated_5>grep -nE 'border-slate-800' index.html | wc -l</automated_5>
    Expected: 0 matches (footer dark border retired).
  </verify>
  <acceptance_criteria>
    - **Source-assertion (V-4 — FINAL dark-surface elimination grep):** `grep -nE '#0F172A|#1E293B|bg-surface-dark|bg-surface-dark-card|bg-slate-(7|8|9)00|--surface-dark-rgb|--surface-dark-card-rgb' index.html` returns 0 matches outside `:root` documentation comments. **V-4 passes.**
    - **Source-assertion (V-5 — FINAL identity-residue grep):** `grep -nE '#4F46E5|#4338CA|#EEF2FF|#A5B4FC|#818CF8|#F8F7FF|Fraunces|bg-indigo|text-indigo|hover:bg-indigo|hover:text-indigo' index.html` returns 0 matches. **V-5 passes.**
    - **Source-assertion (V-8 — token-contract completeness):**
      - `grep -E -- '--brand-soft-rgb|--brand-secondary-rgb' index.html` returns 2+ matches (Plan 01 declarations).
      - `grep -E 'bg-yellow-100|text-yellow-400' index.html` returns 3+ matches (2 bg + 1 text).
      - `grep -nE 'style="[^"]*#[0-9A-Fa-f]{3,6}"' index.html` returns 0 matches (no raw hex in inline styles — Plan 04 Task 1 migrated line 538; Task 2 migrated line 574; line 543 inline dropped entirely).
      - DevTools: `getComputedStyle(document.querySelector('span.bg-yellow-100')).backgroundColor === 'rgb(255, 229, 153)'`. **V-8 passes.**
    - **Behavior-assertion (V-10 per VALIDATION.md):** Walk results → reference matrix → footer. DevTools: `getComputedStyle(document.body).backgroundColor === 'rgb(255, 248, 240)'` AND `Array.from(document.querySelectorAll('section.bg-surface-warm')).every(s => getComputedStyle(s).backgroundColor === 'rgb(255, 248, 240)')` (every D-05 section divider element — iterates all matching sections; avoids brittle bracket-escape in selectors) AND `getComputedStyle(document.querySelector('footer')).backgroundColor === 'rgb(255, 248, 240)'`. All return the same warm off-white. **V-10 passes.**
    - **Behavior-assertion (V-3 partial — visual):** Reference matrix shows PLS badge as Light Yellow with Dark Gray text (readable). PLS card icon bg is Light Yellow with orange stroke. Three 4px orange top-rules visible: above main result card (Task 1), above reference matrix (Task 3), at top of footer (Task 3). Footer renders on warm off-white with `text-slate-500` body copy.
    - **Visual stress test:** Walk all 6 V-9 paths; each result-state string renders with correct callout visibility and no dark surfaces / no indigo / no half-old / half-new state. Defer V-9 to Plan 05 (phase-end gate) — this task does the visual pass to surface any obvious regressions.
  </acceptance_criteria>
  <done>
Reference matrix `<section>` on warm off-white with 4px orange top-rule. PLS badge: Light Yellow bg + Dark Gray text. PLS card icon bg: Light Yellow + orange stroke. Footer: warm off-white + 4px orange top-rule. V-4, V-5, V-8, V-10 all pass. Three 4px orange section dividers visible end-to-end on results page.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ⇄ static HTML | No change. Body markup edits do not introduce new input handling, new external loads, or new JS evaluation paths. |

## STRIDE Threat Register (per SECURITY_CFG=true, ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-05 | Tampering | Cat D markup utility swap + Cat C inline-style migration (lines 533-700) | N/A — no exposure | Pure markup rewrite. Utility class name swaps (`bg-surface-dark` → `bg-surface-elev`, `bg-indigo-100` → `bg-yellow-100`, etc.) trigger different Tailwind-generated CSS but no behavior change. Inline-style values are static brand tokens (no user input interpolation). The `calculateScore()` function at line 938 is NOT edited (EXCL-scoring-or-flow-changes); the `textContent` assignments in `calculateScore()` continue to populate `#result-recommendation` (line 545), `#result-title` (line 541), etc. with the same strings as pre-Phase-2. No XSS surface change. |

**Block-on-high disposition:** No high-severity threats. Block-on-high gate clears.
</threat_model>

<verification>
After all three tasks land:
1. V-4 grep: zero dark-surface references in markup.
2. V-5 grep: zero indigo + Fraunces residue.
3. V-8 grep + DevTools: yellow utilities resolved correctly; no raw hex in inline styles.
4. V-10 DevTools: warm off-white continuous across body + section wrapper + reference matrix + footer.
5. V-3 partial visual: all results-page elements coherently Overdrive. Three 4px orange section dividers visible.
6. Walk all 6 V-9 paths to expose any regression — defer the formal V-9 pass to Plan 05 phase-end gate.

If V-10 returns differing bg values: investigate the section that doesn't match. Most likely culprit: a `bg-white` or `bg-slate-50` leftover utility that wasn't surveyed. Re-grep `grep -nE 'class="[^"]*bg-(white|slate-50)' index.html` and migrate any unintended matches.

If V-8 raw-hex-in-style grep returns matches: the offending line carries `style="...#XXXXXX..."` — investigate and migrate.
</verification>

<success_criteria>
1. Dark hero retired (no `bg-surface-dark` utility anywhere in markup).
2. Main result card renders on white with 3px orange left-border.
3. Both callouts render on white with 5px orange top-strips.
4. Warning icon renders Golden Yellow.
5. PLS badge: Light Yellow bg + Dark Gray text (R-2 contrast fix).
6. PLS card icon bg: Light Yellow.
7. Reference matrix `<section>` and footer both on warm off-white with 4px orange top-rules.
8. Three 4px orange section dividers visible across results page.
9. V-4, V-5, V-8, V-10 all pass. V-3 visual pass.
10. Browser console: zero new errors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-overdrive-default-theme-migration/02-04-SUMMARY.md` documenting:
- Lines changed and their before/after states (1 representative per task).
- DevTools evidence for V-4, V-5, V-8, V-10.
- Visual walk-through evidence (each of 6 V-9 paths reaches results with no dark surfaces / no indigo — Plan 05 confirms scoring strings).
- R-2 contrast override decision audit: PLS badge uses `text-ink` not `text-yellow-400`; rationale; future-Pete pointer.
- D-05(c) decision audit: footer's `border-t-[4px] border-accent` doubles as both the D-04 footer rule AND the D-05(c) divider — one element, two roles.
- Line 677 decision audit: PLS card icon stroke STAYS `text-accent` (orange on yellow), per RESEARCH recommendation. Yellow-on-yellow would be muddy.
- Carry-over to Plan 05: Block 7 (conditional typography per D-13 / R-3) and Block 8 (V-9 scoring regression) — both checkpoint tasks; the executor of Plan 05 needs the visual notes from Plan 02 + Plan 04 to inform Block 7's 0/1/2 markup-edit decision.
</output>
</content>
</invoke>