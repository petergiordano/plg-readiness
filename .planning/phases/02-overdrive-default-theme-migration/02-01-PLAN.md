---
phase: 02-overdrive-default-theme-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - REQ-overdrive-default-theme
user_setup: []

must_haves:
  truths:
    - "Default `:root` block declares the Overdrive accent (`--accent-rgb: 255 144 0;`), Overdrive accent-hover (`--accent-hover-rgb: 230 130 0;`), warm off-white surface (`--surface-rgb: 255 248 240;`), Overdrive Dark Gray text (`--text-rgb: 67 67 67;`), warm-gray neutral ramp, and Space Grotesk display font."
    - "Two new tokens `--brand-soft-rgb: 255 229 153;` (Light Yellow #FFE599) and `--brand-secondary-rgb: 241 194 50;` (Golden Yellow #F1C232) are declared under a new `Secondary palette` divider."
    - "Dark surface tokens `--surface-dark-rgb` and `--surface-dark-card-rgb` are removed from `:root`."
    - "Tailwind config exposes `bg-surface-elev` (white card), `bg-yellow-100` (Light Yellow), `text-yellow-400` (Golden Yellow); the `surface.dark` / `surface.dark-card` utility entries are deleted; `display` fontFamily fallback flips to `'sans-serif'`."
    - "V-2a passes: `--accent-rgb === '255 144 0'`, body bg `=== rgb(255, 248, 240)`."
    - "V-2b passes: `bg-surface-elev` resolves to white in computed styles; `bg-surface-dark`/`bg-surface-dark-card` no longer generate CSS."
  artifacts:
    - path: "index.html"
      provides: "Token contract (`:root` block at lines 48–~83) holding Overdrive values + 2 yellow tokens; Tailwind config exposing `surface.elev` + `yellow.100` + `yellow.400`."
      contains: "--accent-rgb:        255 144 0"
      contains_also: "--brand-secondary-rgb"
      contains_also_2: "elev:        'rgb(var(--surface-elev-rgb)"
      contains_also_3: "yellow: {"
  key_links:
    - from: "index.html `:root` block (lines 48–~83)"
      to: "Tailwind config `colors:` extend block (lines 95–~120)"
      via: "RGB-triplet tokens consumed by `rgb(var(--*-rgb) / <alpha-value>)` template strings"
      pattern: "rgb\\(var\\(--surface-elev-rgb\\)"
    - from: "Tailwind config"
      to: "Markup utilities `bg-accent`, `bg-surface-warm`, `bg-surface-elev`, `bg-yellow-100`, `text-yellow-400`, `text-ink`, `slate-*`"
      via: "Tailwind CDN JIT generation at parse time"
      pattern: "'rgb\\(var\\(--brand-soft-rgb\\)"
---

<objective>
Flip the Phase 1 token contract values to Overdrive defaults AND rewire the inline Tailwind config so the new tokens (`--surface-elev-rgb`, `--brand-soft-rgb`, `--brand-secondary-rgb`) are exposed as utilities, while retiring the `surface.dark` / `surface.dark-card` namespaces. This is the foundation plan — every subsequent plan reads through the new tokens / utilities this plan publishes.

Purpose: Without the token contract flip + config rewire, every downstream change (Cat B migration, results page markup, callout treatments) would render against indigo / dark surfaces. This plan IS the brand switch. Blocks 2 + 3 are tightly coupled (Block 3's `surface.elev` + `yellow.*` entries reference tokens Block 2 declares), so they ship together in this plan.

Output: Two commits inside `index.html` `<head>` (lines 13–~123). Net: ~12 token value flips, 2 token deletions, 2 token additions with new comment divider, `--font-display` value swap, 1 fontFamily fallback flip, 2 Tailwind utility-entry deletions, 2 Tailwind utility-entry additions (`surface.elev`, `yellow.100`/`yellow.400`). Both Task 1 + Task 2 fire the D-14 V-2 execute-phase browser-verify gate (V-2a, V-2b). V-1 has already passed at the orchestrator gate (see 02-VALIDATION.md V-1 Orchestrator Run Log).

**Resolved planner decisions** (Q-1, Q-2, Q-4 from RESEARCH.md; R-1 from RESEARCH.md):
- Q-1: Blocks 2 + 3 ship as ONE plan with two atomic commits (one per Task). Block 4 / 5 / 6 each get their own plan.
- Q-2: `surface.elev` uses the alpha-value pattern — `'rgb(var(--surface-elev-rgb) / <alpha-value>)'` — for symmetry with `surface.warm`.
- Q-4: The `Secondary palette` divider inserts after the Neutral ramp block (after current line 77) and before the Fonts block (before current line 79).
- R-1: `--accent-hover-rgb: 230 130 0;` (~10% darker than `#FF9000` = `#E68200`). Pre-locked in Task 1 to avoid the frankenstein-hover bug on CTAs at lines 324/479/515.
- Q-6: `yellow:` namespace inserts after the `slate:` block, before the closing `}` of `colors:`.

**Threat-model coverage** (see `<threat_model>` below): T-2-01 (token value flips — N/A, CSS values are not user input) and T-2-02 (Tailwind config rewires — N/A, no SRI change, same CDN, same script ordering) are both N/A for this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-overdrive-default-theme-migration/02-CONTEXT.md
@.planning/phases/02-overdrive-default-theme-migration/02-RESEARCH.md
@.planning/phases/02-overdrive-default-theme-migration/02-PATTERNS.md
@.planning/phases/02-overdrive-default-theme-migration/02-VALIDATION.md
@.planning/phases/01-theming-architecture-foundation/01-01-SUMMARY.md
@docs/design/design-system-alpha-overdrive.md
@index.html

<interfaces>
<!-- Pre-Phase-2 :root block (lines 47-83 of index.html). Phase 2 flips values in-place. -->
<!-- Source: re-grepped 2026-05-16 HEAD b1af5f7. -->

Current `:root` block (lines 48-83):
- Lines 50-52: `--accent-rgb` `--accent-hover-rgb` `--accent-muted-rgb`
- Lines 55-58: `--surface-rgb` `--surface-elev-rgb` `--surface-dark-rgb` `--surface-dark-card-rgb`
- Lines 61-62: `--text-rgb` `--text-muted-rgb`
- Line 65:     `--border-rgb`
- Lines 68-77: `--neutral-50-rgb` through `--neutral-900-rgb` (10-stop ramp)
- Lines 80-82: `--font-display` `--font-body` `--font-mono`

Current Tailwind config (lines 86-123):
- Line 92: `display: ['var(--font-display)', 'serif']`
- Lines 102-104: `surface.warm` / `surface.dark` / `surface.dark-card` entries
- Lines 107-117: `slate.50` through `slate.900`
- No `yellow:` namespace yet
- No `surface.elev` entry yet
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip :root token values to Overdrive defaults + add Secondary palette tokens + delete dark tokens</name>
  <files>index.html</files>
  <read_first>
    - index.html (re-grep `:root` open + close anchors before any edit; lines may have drifted from 48/83)
    - .planning/phases/02-overdrive-default-theme-migration/02-CONTEXT.md (D-01 through D-13)
    - .planning/phases/02-overdrive-default-theme-migration/02-RESEARCH.md (## File-Level Scope → Token contract section)
    - .planning/phases/02-overdrive-default-theme-migration/02-PATTERNS.md (P-1: Token declaration in :root + S-2: Comment-divider convention)
    - .planning/phases/01-theming-architecture-foundation/01-01-SUMMARY.md (Token contract — Phase 1 values table)
  </read_first>
  <action>
Edit the `:root` block in `index.html`. Re-grep `:root {` and the closing `}` first; the references below assume the unchanged Phase 1 layout (`:root {` at line 48, closing `}` at line 83). Apply these in sequence so the file remains valid CSS after each change:

(a) Lines 50-52 — accent block flip (per D-01 + R-1):
  - `--accent-rgb:        79 70 229;` → `--accent-rgb:        255 144 0;     /* #FF9000 Overdrive Orange */`
  - `--accent-hover-rgb:  67 56 202;` → `--accent-hover-rgb:  230 130 0;     /* #E68200 ~10% darker (R-1 — closes frankenstein-hover bug on CTAs at 324/479/515) */`
  - `--accent-muted-rgb:  238 242 255;` → `--accent-muted-rgb:  255 241 224;  /* #FFF1E0 soft-orange tint (Claude's-Discretion per CONTEXT D-decisions) */`
  - Update the section comment on line 49 from `/* Accent colors (current indigo) */` → `/* Accent colors (Overdrive orange) */`.

(b) Lines 55-58 — surface block (per D-01 + D-02):
  - `--surface-rgb:           250 250 248;` → `--surface-rgb:           255 248 240;  /* #FFF8F0 warm off-white */`
  - `--surface-elev-rgb:      255 255 255;` — VALUE UNCHANGED. Update inline comment from `/* white — implicit on .answer-card / .check-card */` → `/* #FFFFFF white — consumed by bg-surface-elev (D-02) */`.
  - DELETE line 57 (`--surface-dark-rgb:      15 23 42;`) and line 58 (`--surface-dark-card-rgb: 30 41 59;`). Per D-01 retirement.
  - Update the section comment on line 54 from `/* Surfaces (current warm off-white + dark hero — dark tokens retire in Phase 2) */` → `/* Surfaces (warm off-white + elevated white card; dark surfaces retired per D-01) */`.

(c) Lines 61-62 — text block flip (per D-06):
  - `--text-rgb:        26 26 46;` → `--text-rgb:        67 67 67;      /* #434343 Overdrive Dark Gray (D-06 anchor) */`
  - `--text-muted-rgb:  100 116 139;` → `--text-muted-rgb:  138 138 138;  /* #8A8A8A Overdrive Gray 2 (D-06 anchor) */`
  - Update the section comment on line 60 from `/* Text (current ink + dominant slate-500) */` → `/* Text (Overdrive Dark Gray + Gray 2) */`.

(d) Line 65 — border flip:
  - `--border-rgb: 226 232 240;` → `--border-rgb: 229 229 229;  /* #E5E5E5 Overdrive Border Light (D-06 anchor) */`

(e) Lines 68-77 — neutral ramp flip (per D-06 / D-07). Replace the existing 10-stop slate-default block with this warm-gray ramp (preserve alignment + trailing hex comments):
  - `--neutral-50-rgb:  255 248 240;   /* #FFF8F0 surface anchor */`
  - `--neutral-100-rgb: 245 240 232;   /* #F5F0E8 warm-50 step */`
  - `--neutral-200-rgb: 229 229 229;   /* #E5E5E5 Border Light anchor */`
  - `--neutral-300-rgb: 209 209 209;   /* #D1D1D1 warm-300 step */`
  - `--neutral-400-rgb: 168 168 168;   /* #A8A8A8 warm-400 step */`
  - `--neutral-500-rgb: 138 138 138;   /* #8A8A8A Gray 2 anchor */`
  - `--neutral-600-rgb: 102 102 102;   /* #666666 Gray 1 anchor */`
  - `--neutral-700-rgb: 67 67 67;      /* #434343 Dark Gray anchor */`
  - `--neutral-800-rgb: 51 51 51;      /* #333333 extrapolation */`
  - `--neutral-900-rgb: 26 26 26;      /* #1A1A1A extrapolation (no current Tailwind consumer; declared per D-07 brandable-ramp) */`

(f) Insert Secondary palette block AFTER the neutral ramp closing line (currently 77) and BEFORE the Fonts section comment (currently 79). Use the comment-divider convention from PATTERNS.md S-2 + P-1 (Q-4 recommendation):
  - `            /* ========== Secondary palette ========== */`
  - `            --brand-soft-rgb:      255 229 153;  /* #FFE599 Light Yellow (D-12) */`
  - `            --brand-secondary-rgb: 241 194 50;   /* #F1C232 Golden Yellow (D-12) */`
  - (Add a blank line BEFORE the divider comment to match the existing block-separator style.)

(g) Line 80 — display font swap (per D-13):
  - `--font-display: 'Fraunces', serif;` → `--font-display: 'Space Grotesk', sans-serif;`
  - Update the Fonts section comment on line 79 from `/* Fonts (current identity — Phase 2 swaps Fraunces -> Space Grotesk) */` → `/* Fonts (Overdrive — Space Grotesk display) */`.

Update the section header comment on line 47 from `/* ========== :ROOT DEFAULTS (Overdrive — current Phase 1 values) ========== */` → `/* ========== :ROOT DEFAULTS (Overdrive — Phase 2 production values) ========== */`.

After edits land, commit with message `feat(02-01): flip :root token values to Overdrive defaults`.

Do NOT edit the Tailwind config in this task — that is Task 2. Keeping the commit atomic preserves D-14 V-2a granularity (token contract flip ONLY; config rewire is a separate verify gate).
  </action>
  <verify>
    <automated>grep -nE -- '--accent-rgb:\s+255 144 0|--accent-hover-rgb:\s+230 130 0|--surface-rgb:\s+255 248 240|--text-rgb:\s+67 67 67|--brand-soft-rgb|--brand-secondary-rgb|Space Grotesk' index.html | wc -l</automated>
    Expected: 7 matches (one per asserted token + Space Grotesk).
    <automated_2>grep -nE -- '--surface-dark-rgb|--surface-dark-card-rgb' index.html | grep -v '^[^:]*:[[:space:]]*\(/\*\|//\|#\)' | wc -l</automated_2>
    Expected: 0 matches (both deleted).
    <automated_3>grep -nE 'Fraunces' index.html | wc -l</automated_3>
    Expected: 0 matches (--font-display value flipped; Google Fonts link still references Fraunces — that swaps in Plan 02).
  </verify>
  <acceptance_criteria>
    - **Source-assertion (grep):** `grep -nE -- '--accent-rgb:\s+255 144 0' index.html` returns exactly 1 match on the `:root` declaration line. Same for `--accent-hover-rgb: 230 130 0`, `--surface-rgb: 255 248 240`, `--text-rgb: 67 67 67`, `--brand-soft-rgb: 255 229 153`, `--brand-secondary-rgb: 241 194 50`.
    - **Source-assertion (grep, negative):** `grep -nE -- '^\s*--surface-dark-(rgb|card-rgb)\s*:' index.html` returns 0 matches (both retired tokens deleted from `:root` declarations; comments referencing them in docs blocks are allowed).
    - **Source-assertion (grep, negative for display-font value):** `grep -nE -- "--font-display:\s*'Fraunces'" index.html` returns 0 matches.
    - **Behavior-assertion (V-2a per VALIDATION.md):** browser-load via `python3 -m http.server 8080`; DevTools console: `getComputedStyle(document.documentElement).getPropertyValue('--accent-rgb').trim() === '255 144 0'` AND `getComputedStyle(document.body).backgroundColor === 'rgb(255, 248, 240)'`. **V-2a passes per D-14 execute-phase clause.**
    - **Behavior-assertion (visual):** progress bar (`#progress-fill` line 278) and slide-0 CTA (line 324) render orange (not indigo) on browser reload after this commit; body background is warm off-white. Note: text on the dark hero in the results page WILL look wrong at this point — that is expected interim state (the dark hero retires in Plan 04).
    - **D-14 compliance:** This task touches `<head>` content (the token `<style>` block at lines 13-84). Per D-14 execute-phase clause, V-2a in VALIDATION.md must pass before declaring this task complete. Encode in commit message: `feat(02-01): flip :root token values to Overdrive defaults (V-2a passed)`.
  </acceptance_criteria>
  <done>
The `:root` block declares Overdrive values for accent, accent-hover, accent-muted, surface, text, text-muted, border; the warm-gray neutral ramp is in place; the two yellow tokens are added under a new `Secondary palette` divider; the two dark-surface tokens are removed; `--font-display` resolves to Space Grotesk; V-2a passes in DevTools.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewire Tailwind config — add surface.elev + yellow namespace, delete surface.dark/dark-card, flip display fontFamily fallback</name>
  <files>index.html</files>
  <read_first>
    - index.html (re-grep Tailwind `<script>` open + close anchors at lines 86 + 123; assert `surface.dark` / `surface.dark-card` still present at lines 103-104 before deletion)
    - .planning/phases/02-overdrive-default-theme-migration/02-CONTEXT.md (D-02, D-09, D-12, D-13)
    - .planning/phases/02-overdrive-default-theme-migration/02-RESEARCH.md (## File-Level Scope → Inline Tailwind config section; Q-6 recommendation in Open Questions)
    - .planning/phases/02-overdrive-default-theme-migration/02-PATTERNS.md (P-2: Tailwind config consumer pattern)
  </read_first>
  <action>
Edit the inline Tailwind config `<script>` block (currently lines 86-123). Apply these changes:

(a) Line 92 — display fontFamily fallback flip (per D-13):
  - `display: ['var(--font-display)', 'serif'],` → `display: ['var(--font-display)', 'sans-serif'],`

(b) Lines 102-104 — surface namespace rewrite (per D-02 + D-01):
  - DELETE line 103: `dark:        'rgb(var(--surface-dark-rgb)      / <alpha-value>)',`
  - DELETE line 104: `'dark-card': 'rgb(var(--surface-dark-card-rgb) / <alpha-value>)',`
  - INSERT after the `warm:` line (current line 102), before the closing `}` of `surface:` (current line 105):
    `                            elev:        'rgb(var(--surface-elev-rgb)      / <alpha-value>)',`
    (Preserve column alignment with `warm:` per PATTERNS.md P-2 rule 2 — pad after `elev:` so the `/` column aligns with the existing `warm:` line.)

(c) After the closing `}` of the `slate:` block (current line 118) and BEFORE the closing `}` of the `colors:` extend block (current line 119), insert the new `yellow:` namespace per D-12 + Q-6:
```
                        ,
                        yellow: {
                            100: 'rgb(var(--brand-soft-rgb)      / <alpha-value>)',
                            400: 'rgb(var(--brand-secondary-rgb) / <alpha-value>)',
                        }
```
  - The leading comma is required because the preceding `slate: { ... }` block does NOT have a trailing comma. Add the comma to the end of the `slate:` block's closing brace line OR include it on the new `yellow:` block's opening line — either works as valid JavaScript object syntax. **Recommendation:** add the trailing comma to the `slate:` block's closing `}` line (current line 118) and insert `yellow: { ... }` cleanly after.
  - Per Q-6 + D-12 recommendation: declare ONLY `yellow.100` and `yellow.400` — these are the only stops the markup uses (PLS badge, PLS card icon bg, warning icon). Do NOT declare the full Tailwind yellow palette.

(d) Update the inline comment immediately above the `surface:` block (if any exists) to remove references to dark surfaces. If the current block has no comment, none needed.

After edits, the `colors:` extend block should be ordered: `accent` → `surface` (warm + elev) → `ink` → `slate` → `yellow`. This matches the logical layering per Q-6.

Commit with message: `feat(02-01): rewire Tailwind config — add surface.elev + yellow.{100,400}, retire surface.dark/dark-card (V-2b passed)`.
  </action>
  <verify>
    <automated>grep -nE "elev:\s+'rgb\(var\(--surface-elev-rgb\)" index.html | wc -l</automated>
    Expected: 1 match (new surface.elev entry).
    <automated_2>grep -nE "'rgb\(var\(--surface-dark-rgb\)|'rgb\(var\(--surface-dark-card-rgb\)" index.html | wc -l</automated_2>
    Expected: 0 matches (both Tailwind config entries deleted).
    <automated_3>grep -nE "'rgb\(var\(--brand-soft-rgb\)|'rgb\(var\(--brand-secondary-rgb\)" index.html | wc -l</automated_3>
    Expected: 2 matches (yellow.100 + yellow.400 entries).
    <automated_4>grep -nE "display:\s*\['var\(--font-display\)',\s*'sans-serif'\]" index.html | wc -l</automated_4>
    Expected: 1 match (fallback flip).
  </verify>
  <acceptance_criteria>
    - **Source-assertion (grep, positive):** All four `<automated>` greps above return their expected match counts.
    - **Source-assertion (grep, negative):** `grep -nE "surface\.dark|surface\.dark-card|'dark':|'dark-card':" index.html` returns 0 matches inside the Tailwind config block (lines 86-~125 after edit).
    - **Behavior-assertion (V-2b per VALIDATION.md):** browser-load; DevTools console: zero red errors at load. Console: `Array.from(document.querySelectorAll('*')).filter(e => { const s = getComputedStyle(e); return e.matches('.bg-surface-dark, .bg-surface-dark-card') && s.backgroundColor !== 'rgba(0, 0, 0, 0)'; }).length` returns 0 (the deleted utility entries produce no CSS for any element still referencing them; element bg falls through to body / parent). **V-2b passes per D-14 execute-phase clause.**
    - **Behavior-assertion (post-edit DevTools, interim state):** Tailwind CDN generates `.bg-surface-elev { background-color: rgb(var(--surface-elev-rgb)) }` — verifiable by running `Array.from(document.styleSheets).map(s => Array.from(s.cssRules || []).find(r => r.selectorText === '.bg-surface-elev'))` and finding one non-null result. **NOTE:** Tailwind CDN only generates utility CSS for class names that appear in the markup. At this point in the sequence, no markup uses `bg-surface-elev` yet (that's Plan 04 / Block 6). The CSS rule may NOT appear in the stylesheet until markup consumes it — this is expected. Verify Tailwind config availability instead via: `tailwind.config.theme.extend.colors.surface.elev` returns the literal template string.
    - **Behavior-assertion (yellow utility):** Same caveat — no markup uses `bg-yellow-100` yet (lands in Plan 04). Verify config availability: `tailwind.config.theme.extend.colors.yellow[100]` returns the literal `'rgb(var(--brand-soft-rgb)      / <alpha-value>)'`.
    - **D-14 compliance:** This task touches `<head>` content (the inline Tailwind config `<script>`). V-2b in VALIDATION.md must pass before declaring complete.
    - **plan-checker-watch annotation (per planning_context):** VALIDATION.md V-1(e) wording should be hardened to "after probe triggers Space Grotesk render" — the orchestrator's V-1 run log noted that `document.fonts.check('1em "Space Grotesk")` returns `false` until an element demands the face. Not blocking for this task; flagged for orchestrator to surface in any future VALIDATION.md revision.
  </acceptance_criteria>
  <done>
Tailwind config exposes `bg-surface-elev`, `bg-yellow-100`, `text-yellow-400` (verifiable by `tailwind.config.theme.extend.colors` inspection in DevTools); `surface.dark` and `surface.dark-card` are deleted; the `display` fontFamily fallback is `'sans-serif'`; V-2b passes (zero JS errors at load); no dark-surface utility generates CSS for any element still referencing the now-deleted names.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ⇄ static HTML | Single-file static page served via `python3 -m http.server`. No backend, no API, no user-input ingestion path crosses this edit. |
| browser ⇄ Tailwind CDN (`cdn.tailwindcss.com`) | Pre-existing CDN script load from Phase 1. NOT modified in this plan. |
| browser ⇄ Google Fonts CDN | Pre-existing `<link>` from Phase 1. NOT modified in this plan (Plan 02 modifies it). |

## STRIDE Threat Register (per SECURITY_CFG=true, ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Tampering | `:root` token value flips (Task 1) | N/A — no exposure | CSS custom-property values are not user input. There is no JS pathway in this app that writes to `:root` based on URL/cookie/POST data. The FOUC `<script>` at lines 7-12 reads `?client=<slug>` only to set `data-theme` on `<html>` — it does not write CSS values. |
| T-2-02 | Tampering | Tailwind config rewires (Task 2) | N/A — no exposure | Same trust boundary as Phase 1. No SRI change. Same CDN URL (`https://cdn.tailwindcss.com`). Same script ordering (FOUC → token-style → CDN → config → fonts → component-style, per 01-01-SUMMARY.md). No new third-party dependency added. |
| T-2-03..T-2-05 | — | — | Out of scope for this plan | Covered in Plans 02, 03, 04 respectively. |

**Block-on-high disposition:** No high-severity threats identified. Block-on-high gate clears.
</threat_model>

<verification>
After both tasks land:
1. Run V-2a (Task 1) and V-2b (Task 2) per VALIDATION.md — each as a stand-alone post-commit check (NOT a combined sweep, per D-14).
2. Run V-4 (dark-surface grep): `grep -nE '#0F172A|#1E293B|--surface-dark-rgb|--surface-dark-card-rgb' index.html` → expect 0 matches in `:root` and Tailwind config blocks. (Comments in docs blocks may still mention `dark-card`; those are documentation, not active CSS, and are acceptable.)
3. Visual interim check: progress bar renders orange; body bg is warm off-white. The results page (lines 533-700) will still render the OLD dark hero — that is expected interim state; Plan 04 retires it.
4. Browser console: zero red errors at load. (One expected pre-existing Tailwind CDN production WARNING — same as Phase 1 baseline + V-1 run log.)

If any V-2a or V-2b assertion fails: STOP and investigate before proceeding to Plan 02. Per D-14 cadence, do NOT batch verifies.
</verification>

<success_criteria>
1. `:root` block (lines 48-~85 after Secondary palette insertion) declares Overdrive values for all 7 base color tokens + warm-gray neutral ramp + Space Grotesk display font + 2 yellow tokens. `--surface-dark-rgb` and `--surface-dark-card-rgb` are removed.
2. Tailwind config exposes `bg-surface-elev`, `bg-yellow-100`, `text-yellow-400`; `surface.dark` and `surface.dark-card` are removed; `display` fontFamily fallback is `'sans-serif'`.
3. V-2a passes (Task 1 commit).
4. V-2b passes (Task 2 commit).
5. Both commits are atomic — Task 1 changes ONLY the token contract `<style>`; Task 2 changes ONLY the Tailwind config `<script>`. No cross-mixing.
6. Browser-load shows zero red console errors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-overdrive-default-theme-migration/02-01-SUMMARY.md` documenting:
- Exact final values of all flipped tokens (Overdrive Orange, warm off-white, Dark Gray, etc.)
- New utility names exposed (`bg-surface-elev`, `bg-yellow-100`, `text-yellow-400`)
- Retired utility names (`bg-surface-dark`, `bg-surface-dark-card`)
- V-2a + V-2b passing evidence (DevTools console output snippets)
- Open carry-over to Plan 02: Google Fonts `<link>` at line 124 still loads Fraunces — that swap is Plan 02 / Block 4.
- Open carry-over to Plan 03: Component `<style>` block (lines 125-272) still has Cat B indigo/slate literals — Plan 03 / Block 5 migrates them.
- Open carry-over to Plan 04: Results page (lines 533-700) still uses `bg-surface-dark` / `bg-surface-dark-card` markup — currently renders bg-less but unchanged-looking-dark because the old `--text-rgb` is now light-gray on a no-bg surface. Plan 04 / Block 6 retires the dark-hero markup.
</output>
</content>
</invoke>