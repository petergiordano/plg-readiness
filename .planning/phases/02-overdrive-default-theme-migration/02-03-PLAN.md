---
phase: 02-overdrive-default-theme-migration
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - index.html
autonomous: true
requirements:
  - REQ-overdrive-default-theme
user_setup: []

must_haves:
  truths:
    - "Component `<style>` block (lines 125-272) has zero raw hex literals for the retired indigo identity (`#4F46E5`, `#A5B4FC`, `#818CF8`, `#EEF2FF`, `#F8F7FF`)."
    - "Resting borders, key-badge borders, and meta-text colors resolve through `--border-rgb` / `--neutral-300-rgb` / `--neutral-400-rgb` / `--text-muted-rgb` tokens — re-themable per Phase 1 D-11."
    - "Hover states on `.answer-card`, `.check-card`, and `.answer-card .key-badge` derive from `--accent-rgb` via 0.06 (bg) / 0.4 (border) alpha — locked across themes per D-11."
    - "Selected states on `.answer-card`, `.check-card`, `.check-box`, `.answer-card .key-badge` resolve through `--accent-rgb` (border / bg) + `--accent-muted-rgb` (bg fill) + `--surface-elev-rgb` (key-badge bg) per Phase 1 SUMMARY hand-off catalog."
    - "V-2d passes: hovering an `.answer-card` on slide 1 produces `getComputedStyle().backgroundColor === 'rgba(255, 144, 0, 0.06)'` and `borderColor === 'rgba(255, 144, 0, 0.4)'`."
    - "V-6 passes (same as V-2d): hover state is alpha-derived from accent, not literal indigo."
  artifacts:
    - path: "index.html"
      provides: "Component `<style>` block (lines 125-272) with all 18 Cat B literal sites migrated to var-driven references; hovers alpha-derived per D-11."
      contains: "rgb(var(--accent-rgb) / 0.06)"
      contains_also: "rgb(var(--accent-rgb) / 0.4)"
      contains_also_2: "rgb(var(--surface-elev-rgb))"
  key_links:
    - from: ".answer-card:hover / .check-card:hover (component `<style>`)"
      to: "--accent-rgb (token contract, lines 50)"
      via: "rgb(var(--accent-rgb) / 0.06) and / 0.4 alpha-derived form"
      pattern: "rgb\\(var\\(--accent-rgb\\) / 0\\."
    - from: ".answer-card.selected / .check-card.selected / .check-box / .key-badge"
      to: "--accent-rgb + --accent-muted-rgb + --surface-elev-rgb tokens"
      via: "rgb(var(--*-rgb)) opaque form"
      pattern: "rgb\\(var\\(--accent-(rgb|muted-rgb)\\)\\)"
---

<objective>
Migrate the 18 Category B literal sites in the component `<style>` block (lines 125-272) from raw indigo / slate hex to var-driven `rgb(var(--*-rgb))` references. This implements the Phase 1 SUMMARY hand-off catalog (which D-08 zero-visual-diff held as literals in Phase 1) and the D-11 alpha-derived hover treatment (3 sites get the new pattern Phase 2 establishes).

Purpose: Without this migration, hovering an answer card on slide 1 would render `#A5B4FC` indigo on the new orange-accented page — a frankenstein bug failing acceptance criterion #3 (no half-old / half-new state). Selected states would render `#4F46E5` indigo border / `#EEF2FF` indigo-muted bg on orange-everything-else — same frankenstein. Cat B is where the SLIDE-LEVEL identity coherence lives; without this plan, the slides look broken before the user ever reaches the results page.

Output: One commit. ~18 line edits inside `<style>` block (lines 125-272). Fires V-2d (per BV-1 / per VALIDATION.md) AND V-6 (D-11 hover correctness).

**Resolved planner decisions:**
- Q-3 (rewire inline `background: white` on lines 192 + 262 to `rgb(var(--surface-elev-rgb))`): YES — per planning_context "Strong recommendation: rewire" + RESEARCH Q-3 rationale. Phase 2's whole point is brandable cross-theme consistency; leaving `white` literal creates a hole in the contract.
- Line 240 `.result-overline` color: use `rgb(var(--text-muted-rgb))` (NOT `--neutral-500-rgb`) — semantic intent is "muted text" per PATTERNS.md P-3d note. Both tokens hold the same value (`138 138 138`) but the semantic slug should match intent.
- Line 251 `.phase-rule` border-top: keep the alpha pattern. Migrate from `rgba(79,70,229,0.2)` → `rgb(var(--accent-rgb) / 0.2)` per PATTERNS.md P-3d. Do NOT use the Tailwind utility `bg-accent/20` (this is raw CSS, not a utility class).

**Threat-model coverage:** T-2-04 (Cat B literal migration) — N/A; pure CSS value rewrite, no behavior change, no new input handling. See `<threat_model>` below.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-overdrive-default-theme-migration/02-CONTEXT.md
@.planning/phases/02-overdrive-default-theme-migration/02-RESEARCH.md
@.planning/phases/02-overdrive-default-theme-migration/02-PATTERNS.md
@.planning/phases/02-overdrive-default-theme-migration/02-VALIDATION.md
@.planning/phases/02-overdrive-default-theme-migration/02-01-PLAN.md
@.planning/phases/02-overdrive-default-theme-migration/02-02-PLAN.md
@.planning/phases/01-theming-architecture-foundation/01-01-SUMMARY.md
@index.html

<interfaces>
<!-- Phase 1 SUMMARY Cat B hand-off catalog (01-01-SUMMARY.md lines 124-139): -->
<!-- Lines verified at write-time 2026-05-16 HEAD b1af5f7; re-grep before executing. -->

Cat B sites (18 total) — current literal -> target var reference, grouped by sub-pattern P-3a..P-3e:

P-3a (resting borders):
- Line 169 `border: 1.5px solid #E2E8F0;` (.answer-card)            -> rgb(var(--border-rgb))
- Line 201 `border: 1.5px solid #E2E8F0;` (.check-card)             -> rgb(var(--border-rgb))
- Line 182 `border: 1.5px solid #CBD5E1;` (.key-badge)              -> rgb(var(--neutral-300-rgb))
- Line 215 `border: 1.5px solid #CBD5E1;` (.check-box)              -> rgb(var(--neutral-300-rgb))
- Line 262 `border: 1px solid #CBD5E1; ... background: white;` (.kbd-hint span) -> border rgb(var(--neutral-300-rgb)); bg rgb(var(--surface-elev-rgb))

P-3b (hover states — D-11 alpha-derived; this is the NEW pattern Phase 2 establishes):
- Line 177 `.answer-card:hover { border-color: #A5B4FC; background: #F8F7FF; }` -> border-color rgb(var(--accent-rgb) / 0.4); background rgb(var(--accent-rgb) / 0.06)
- Line 190 `.answer-card:hover .key-badge { border-color: #818CF8; color: #818CF8; }` -> both rgb(var(--accent-rgb) / 0.4)
- Line 210 `.check-card:hover { border-color: #A5B4FC; background: #F8F7FF; }` -> border-color rgb(var(--accent-rgb) / 0.4); background rgb(var(--accent-rgb) / 0.06)

P-3c (selected states — Phase 1 SUMMARY catalog):
- Line 178 `.answer-card.selected { border-color: #4F46E5; background: #EEF2FF; }` -> border-color rgb(var(--accent-rgb)); background rgb(var(--accent-muted-rgb))
- Line 211 `.check-card.selected { border-color: #4F46E5; background: #EEF2FF; }`   -> border-color rgb(var(--accent-rgb)); background rgb(var(--accent-muted-rgb))
- Line 221 `.check-card.selected .check-box { border-color: #4F46E5; background: #4F46E5; }` -> both rgb(var(--accent-rgb))
- Line 192 `.answer-card.selected .key-badge { border-color: #4F46E5; color: #4F46E5; background: white; }` -> border-color + color rgb(var(--accent-rgb)); background rgb(var(--surface-elev-rgb))

P-3d (text colors + accent rule):
- Line 186 `.key-badge { ... color: #94A3B8; }`                      -> rgb(var(--neutral-400-rgb))
- Line 258 `.kbd-hint { ... color: #94A3B8; }`                       -> rgb(var(--neutral-400-rgb))
- Line 240 `.result-overline { color: #64748B; }`                    -> rgb(var(--text-muted-rgb))  [semantic intent: "muted text"]
- Line 246 `.phase-overline { color: #4F46E5; }`                     -> rgb(var(--accent-rgb))
- Line 251 `.phase-rule { border-top: 1px solid rgba(79,70,229,0.2); }` -> border-top: 1px solid rgb(var(--accent-rgb) / 0.2)

P-3e (.slide background — line 137):
- Line 137 `.slide { ... background: #FAFAF8; }`                     -> rgb(var(--surface-rgb))
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate resting borders + text colors + .slide bg + .phase-rule (P-3a, P-3d, P-3e patterns)</name>
  <files>index.html</files>
  <read_first>
    - index.html (re-grep each cited line anchor — open + close — before editing; Phase 1 SUMMARY notes line numbers may drift)
    - .planning/phases/02-overdrive-default-theme-migration/02-CONTEXT.md (D-06, D-11)
    - .planning/phases/02-overdrive-default-theme-migration/02-RESEARCH.md (## File-Level Scope → Component <style> block table)
    - .planning/phases/02-overdrive-default-theme-migration/02-PATTERNS.md (P-3a, P-3d, P-3e)
    - .planning/phases/01-theming-architecture-foundation/01-01-SUMMARY.md (Category B table at lines 124-139)
  </read_first>
  <action>
Edit the component `<style>` block (lines 125-272). Re-grep each anchor before changing — Plan 01's Secondary palette insert shifted lines 13-84 only, so component-style lines should be unchanged from the catalog, but verify.

Migrate these 10 sites (P-3a + P-3d + P-3e) in a single commit:

(a) Line 137 (`.slide` background): change literal `#FAFAF8` → `rgb(var(--surface-rgb))`.
(b) Line 169 (`.answer-card` border): change literal `#E2E8F0` → `rgb(var(--border-rgb))`.
(c) Line 182 (`.key-badge` border): change literal `#CBD5E1` → `rgb(var(--neutral-300-rgb))`.
(d) Line 186 (`.key-badge` color): change literal `#94A3B8` → `rgb(var(--neutral-400-rgb))`.
(e) Line 201 (`.check-card` border): change literal `#E2E8F0` → `rgb(var(--border-rgb))`.
(f) Line 215 (`.check-box` border): change literal `#CBD5E1` → `rgb(var(--neutral-300-rgb))`.
(g) Line 240 (`.result-overline` color): change literal `#64748B` → `rgb(var(--text-muted-rgb))`. Per planner decision in `<objective>`: use the semantic `--text-muted-rgb` token, not `--neutral-500-rgb`.
(h) Line 246 (`.phase-overline` color): change literal `#4F46E5` → `rgb(var(--accent-rgb))`.
(i) Line 251 (`.phase-rule` border-top): change `rgba(79,70,229,0.2)` → `rgb(var(--accent-rgb) / 0.2)`. Keep the `1px solid` prefix and the trailing `;` unchanged.
(j) Line 258 (`.kbd-hint` color): change literal `#94A3B8` → `rgb(var(--neutral-400-rgb))`.

Use single-property edits — DO NOT collapse multi-property rules. Each line above contains one or a few CSS declarations; preserve surrounding whitespace and other declarations exactly.

Commit message: `feat(02-03): migrate Cat B resting/text/border literals to var references`.
  </action>
  <verify>
    <automated>grep -nE '#FAFAF8|#E2E8F0|#CBD5E1|#94A3B8|#64748B' index.html | grep -v '/\*' | grep -v '^[[:space:]]*--' | wc -l</automated>
    Expected: 0 matches outside of `:root` declarations and CSS comments. (The `:root` token comments at lines 55, 65, 66, etc. still contain hex examples; the negative grep filters those out via `grep -v '/\*'` and `--` prefix.)
    <automated_2>grep -nE 'rgb\(var\(--border-rgb\)\)|rgb\(var\(--neutral-300-rgb\)\)|rgb\(var\(--neutral-400-rgb\)\)|rgb\(var\(--text-muted-rgb\)\)|rgb\(var\(--accent-rgb\) / 0\.2\)' index.html | wc -l</automated_2>
    Expected: 10+ matches (one per migrated site).
    <automated_3>grep -nE '#4F46E5' index.html | wc -l</automated_3>
    Expected: 4 matches remaining (lines 177/178/190/192/210/211/221 hover + selected literals — Task 2 handles those). Actually Task 1 migrates line 246 (`.phase-overline`) which currently uses `#4F46E5`, so the count after Task 1 should be 6 (177/178/190/192/210/211/221 minus 246, but lines 177/190/210 use indigo variants like `#A5B4FC`/`#818CF8`/`#F8F7FF`, NOT `#4F46E5` — so the actual count after Task 1 is the count of `#4F46E5` in lines 178/192/211/221 = 4 + line 251's `rgba(79,70,229...)` decimal form, which doesn't match `#4F46E5` grep. Expected after Task 1: ~4 raw `#4F46E5` matches remaining (lines 178/192/211/221), all of which Task 2 handles.
  </verify>
  <acceptance_criteria>
    - **Source-assertion (grep, positive):** First two `<automated>` greps return their expected counts after Task 1 commits.
    - **Source-assertion (grep, semantic):** `grep -n 'background: rgb(var(--surface-rgb))' index.html` returns 1 match on line ~137 (`.slide` rule).
    - **Source-assertion (grep, alpha pattern):** `grep -nE 'rgb\(var\(--accent-rgb\) / 0\.2\)' index.html` returns 1 match on line ~251 (`.phase-rule`).
    - **Behavior-assertion (visual):** Browser-load. Slide 1 answer cards show subtle warm borders (the `--border-rgb` value `229 229 229` resolves through the new neutral ramp); key-badge borders show slightly darker neutral; result-overline + phase-rule text colors resolve through the new warm-gray Dark Gray / accent values.
    - **Behavior-assertion (key check):** The `.phase-overline` text on the results page renders ORANGE (was indigo) after this commit. This is a low-cost visual signal that Task 1 landed correctly.
    - **D-14 compliance:** This task touches `<head>` content (the component `<style>` block at lines 125-272). The `<head>` ordering does NOT change, but per D-14's literal text — "after every task that modifies `<head>`" — V-2d applies. The hover-state portion of V-2d cannot pass until Task 2 lands (since hover behavior is what Task 2 migrates). For Task 1: encode partial V-2d — verify computed background of `.slide` returns `rgb(255, 248, 240)` and `.phase-overline` returns `rgb(255, 144, 0)`. Full V-2d (hover assertion) waits for Task 2.
  </acceptance_criteria>
  <done>
All 10 P-3a + P-3d + P-3e literal sites in the component `<style>` block resolve through `--*-rgb` tokens. Browser shows orange `.phase-overline` on the results page; warm-gray neutral borders on slide answer cards; `.slide` background is warm off-white (continuous with body). No raw `#FAFAF8` / `#E2E8F0` / `#CBD5E1` / `#94A3B8` / `#64748B` literals remain outside `:root` comments.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate hover + selected states (P-3b D-11 alpha-derived; P-3c selected-state Phase 1 catalog)</name>
  <files>index.html</files>
  <read_first>
    - index.html (re-grep lines 177/178/190/192/210/211/221/262 — confirm current literals match the catalog before editing)
    - .planning/phases/02-overdrive-default-theme-migration/02-CONTEXT.md (D-11 alpha-derived hover rule)
    - .planning/phases/02-overdrive-default-theme-migration/02-RESEARCH.md (## File-Level Scope Component <style> table; ## Validation Architecture V-6)
    - .planning/phases/02-overdrive-default-theme-migration/02-PATTERNS.md (P-3b, P-3c — alpha-derived hover and selected-state rewrite forms)
    - .planning/phases/02-overdrive-default-theme-migration/02-VALIDATION.md (V-2d, V-6)
    - .planning/phases/01-theming-architecture-foundation/01-01-SUMMARY.md (Cat B catalog rows for lines 177/178/190/192/210/211/221)
  </read_first>
  <action>
Edit the component `<style>` block. Migrate these 8 sites (P-3b hover + P-3c selected + the rewire on line 262):

P-3b (hover states — D-11 alpha-derived form):
(a) Line 177 (`.answer-card:hover`): change `border-color: #A5B4FC; background: #F8F7FF;` → `border-color: rgb(var(--accent-rgb) / 0.4); background: rgb(var(--accent-rgb) / 0.06);`
(b) Line 190 (`.answer-card:hover .key-badge`): change `border-color: #818CF8; color: #818CF8;` → both to `rgb(var(--accent-rgb) / 0.4)`.
(c) Line 210 (`.check-card:hover`): change `border-color: #A5B4FC; background: #F8F7FF;` → `border-color: rgb(var(--accent-rgb) / 0.4); background: rgb(var(--accent-rgb) / 0.06);`.

P-3c (selected states — Phase 1 catalog):
(d) Line 178 (`.answer-card.selected`): change `border-color: #4F46E5; background: #EEF2FF;` → `border-color: rgb(var(--accent-rgb)); background: rgb(var(--accent-muted-rgb));`.
(e) Line 192 (`.answer-card.selected .key-badge`): change `border-color: #4F46E5; color: #4F46E5; background: white;` → `border-color: rgb(var(--accent-rgb)); color: rgb(var(--accent-rgb)); background: rgb(var(--surface-elev-rgb));`. Per Q-3 planner decision: rewire `background: white` to the `--surface-elev-rgb` token for cross-theme consistency.
(f) Line 211 (`.check-card.selected`): change `border-color: #4F46E5; background: #EEF2FF;` → `border-color: rgb(var(--accent-rgb)); background: rgb(var(--accent-muted-rgb));`.
(g) Line 221 (`.check-card.selected .check-box`): change `border-color: #4F46E5; background: #4F46E5;` → both to `rgb(var(--accent-rgb))`.

Inline `background: white` on `.kbd-hint span`:
(h) Line 262 (`.kbd-hint span`): in the rule that currently includes `border: 1px solid #CBD5E1; ... background: white;`, change `background: white;` → `background: rgb(var(--surface-elev-rgb));`. (The border rewire from `#CBD5E1` → `rgb(var(--neutral-300-rgb))` is part of THIS task as well — verify the border literal didn't get caught by Task 1's edits; if Task 1's regex picked it up, skip the border change here.)

Preserve all surrounding whitespace and rule structure exactly. Do NOT collapse the per-property declarations onto one line; preserve the existing formatting style.

Commit message: `feat(02-03): migrate Cat B hover + selected states to alpha-derived var references (V-2d + V-6 passed)`.
  </action>
  <verify>
    <automated>grep -nE '#A5B4FC|#818CF8|#F8F7FF' index.html | wc -l</automated>
    Expected: 0 matches (all retired indigo hover literals removed).
    <automated_2>grep -nE 'rgb\(var\(--accent-rgb\) / 0\.4\)' index.html | wc -l</automated_2>
    Expected: 4+ matches (3 from hover P-3b — lines 177/190/210 each contain the alpha; line 190 contains it twice for border-color + color).
    <automated_3>grep -nE 'rgb\(var\(--accent-rgb\) / 0\.06\)' index.html | wc -l</automated_3>
    Expected: 2 matches (lines 177 + 210 bg).
    <automated_4>grep -nE 'rgb\(var\(--accent-muted-rgb\)\)' index.html | wc -l</automated_4>
    Expected: 2 matches (lines 178 + 211 selected-state bg).
    <automated_5>grep -nE '#4F46E5|#EEF2FF' index.html | wc -l</automated_5>
    Expected: 0 matches outside `:root` doc comments. (Line 538 inline-style — Cat C — still carries `#4F46E5` until Plan 04; if this grep still returns 1 match for line 538 after Task 2, that's expected.)
  </verify>
  <acceptance_criteria>
    - **Source-assertion (grep, V-5 alignment):** `grep -nE 'rgb\(var\(--accent-rgb\) / 0\.06\)' index.html` returns 2 matches (lines 177 + 210). `grep -nE 'rgb\(var\(--accent-rgb\) / 0\.4\)' index.html` returns 4 matches (line 177 border, line 190 border + color, line 210 border).
    - **Source-assertion (grep, negative):** `grep -nE '#A5B4FC|#818CF8|#F8F7FF' index.html` returns 0 matches.
    - **Source-assertion (grep, negative — selected literals):** `grep -nE '#EEF2FF' index.html | grep -v '/\*'` returns 0 matches (selected-state muted bg literal retired).
    - **Behavior-assertion (V-2d per VALIDATION.md):** browser-load via `python3 -m http.server 8080`; navigate to slide 1; hover over an answer-card; DevTools Elements panel → Computed tab. Assert `getComputedStyle(hoveredCard).backgroundColor === 'rgba(255, 144, 0, 0.06)'`; `borderColor === 'rgba(255, 144, 0, 0.4)'`. Browsers normalize `rgb(R G B / α)` to `rgba(R, G, B, α)` in computed styles — both forms are equivalent. **V-2d passes per D-14 execute-phase clause.**
    - **Behavior-assertion (V-6 per VALIDATION.md):** Same as V-2d — V-6 is the canonical D-11 hover-state correctness check. Hover state on `.answer-card` AND `.check-card` (slide 4 / slide 5) renders alpha-derived from orange. Click an answer-card to trigger selected state: `border-color === 'rgb(255, 144, 0)'`; `backgroundColor === 'rgb(255, 241, 224)'` (the new `--accent-muted-rgb` value from Plan 01). **V-6 passes.**
    - **Behavior-assertion (visual):** On slide 1, hovering an answer card shows a faint orange tint with an orange-ish border. Clicking it shows a stronger orange border with a soft warm-orange tinted bg. NO indigo / blue tints anywhere.
    - **D-14 compliance:** This task touches `<head>` content. V-2d + V-6 both pass before declaring complete.
  </acceptance_criteria>
  <done>
All Cat B hover + selected literals (8 sites) resolve through `--accent-rgb` (opaque or alpha-derived) + `--accent-muted-rgb` + `--surface-elev-rgb`. V-2d passes (hover renders alpha-derived orange). V-6 passes (D-11 correctness). No indigo literals remain in the component `<style>` block.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ⇄ static HTML | No change from Phase 1. Component `<style>` modifications are pure CSS rewrites. |

## STRIDE Threat Register (per SECURITY_CFG=true, ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-04 | Tampering / Information Disclosure | Cat B literal migration (component `<style>` block) | N/A — no exposure | Pure CSS value rewrite. No selectors added, no `@import`, no `url(...)` references, no JS evaluation path crosses this edit. CSS custom property substitution is a parse-time CSS feature — not a script execution. |

**Block-on-high disposition:** No high-severity threats. Block-on-high gate clears.
</threat_model>

<verification>
1. Run V-2d (hover assertion) after Task 2 commits.
2. Run V-6 (D-11 hover correctness) — same assertion path as V-2d.
3. Run V-5 grep (negative identity-residue): `grep -nE '#4F46E5|#4338CA|#EEF2FF|#A5B4FC|#818CF8|#F8F7FF|bg-indigo|text-indigo|hover:bg-indigo|hover:text-indigo' index.html` — expected: only line 538's inline-style `#4F46E5` may remain (Plan 04 migrates it). All Cat B literals retired.
4. Walk slides 1, 2, 3 (radio) + slides 4, 5 (checkbox) — hover + click + de-select every answer/check card. No indigo / blue tints anywhere. All hover + selected states render orange (opaque or alpha-derived).

If V-2d or V-6 fails: inspect the line edited and confirm the alpha value matches (0.06 vs 0.04, 0.4 vs 0.5 — common transcription errors).
</verification>

<success_criteria>
1. All 18 Cat B sites (10 in Task 1, 8 in Task 2) migrated to var-driven references.
2. V-2d passes (hover renders alpha-derived orange).
3. V-6 passes (D-11 alpha-derived correctness).
4. V-5 grep returns 0 matches for `#A5B4FC|#818CF8|#F8F7FF|#EEF2FF` and only line-538 leftover for `#4F46E5` (which Plan 04 migrates).
5. Slides 1-5 render coherently with orange hovers + selected states.
6. Browser console shows zero new errors at load.
</success_criteria>

<output>
After completion, create `.planning/phases/02-overdrive-default-theme-migration/02-03-SUMMARY.md` documenting:
- The 18 Cat B sites migrated, with before / after snippets (1 representative per pattern P-3a..P-3e).
- V-2d + V-6 DevTools evidence (computed-style values for hover + selected on slide 1).
- The Q-3 decision audit: line 192 + line 262 `background: white` rewired to `rgb(var(--surface-elev-rgb))` — wires the Phase-1-declared `--surface-elev-rgb` token to its second consumer (the first is Plan 04 / Task 1 main result card).
- Open carry-over to Plan 04: line 538 inline-style `border-left: 3px solid #4F46E5` still has indigo literal — Plan 04 / Task 1 migrates it. This is the ONLY remaining `#4F46E5` in the file as of Plan 03 ship time.
- Visual confirmation that slides 1-5 are now end-to-end Overdrive (warm bg + orange accents); only the results page region still has dark hero / indigo callouts (Plan 04 retires).
</output>
</content>
</invoke>