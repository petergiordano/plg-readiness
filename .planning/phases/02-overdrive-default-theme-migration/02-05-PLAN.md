---
phase: 02-overdrive-default-theme-migration
plan: 05
type: execute
wave: 5
depends_on: ["02-01", "02-02", "02-03", "02-04"]
files_modified:
  - index.html
autonomous: false
requirements:
  - REQ-overdrive-default-theme
user_setup: []

must_haves:
  truths:
    - "Per D-13, the typography weight/size decision (0/1/2 markup edits) is made AFTER Space Grotesk renders in the browser — not pre-locked. This plan encodes that decision as a checkpoint."
    - "V-9 scoring regression passes: every one of 6 representative answer paths produces the same result string + same callout visibility as the pre-Phase-2 baseline."
    - "V-3 visual identity coherence passes: the post-Phase-2 app is recognizably Overdrive end-to-end per design system §9."
    - "V-7 font-load + fallback passes (re-verified at phase end after all <head>-touching commits land)."
    - "Phase-end residue greps (V-4 + V-5) return zero matches."
  artifacts:
    - path: "index.html"
      provides: "Final Phase 2 file state: zero typography edits (most likely per D-13 minimum-viable bound) OR one edit (line 303 hero size+weight bump) OR two edits (line 303 + lines 344/386/421/463/499 weight bumps). Decision made at Task 1 checkpoint."
  key_links:
    - from: "Plan 05 Task 1 checkpoint decision"
      to: "D-13 minimum-viable typography bound"
      via: "human eye-check of Space Grotesk render"
      pattern: "(no markup change|font-size bump|font-weight bump)"
    - from: "Plan 05 Task 2 V-9 scoring regression check"
      to: "function calculateScore() at index.html line 938"
      via: "manual quiz walk-through with 6-path matrix"
      pattern: "calculateScore"
---

<objective>
Close out Phase 2 with the two remaining gates: (1) the conditional D-13 typography decision (R-3) and (2) the V-9 scoring regression check (the load-bearing acceptance criterion #4 gate).

Both are CHECKPOINT tasks — they require human eye-check + interactive quiz walk-through, not automation. Task 1 is `checkpoint:decision` (the executor pauses for the user to decide 0/1/2 markup edits based on what they see in the browser). Task 2 is `checkpoint:human-verify` (the executor pauses for the user to confirm all 6 V-9 paths produce identical result strings + callout visibility vs the pre-Phase-2 baseline).

Purpose: D-13 explicitly states typography adjustments are post-browser-verify-determined, not pre-locked. R-3 (RESEARCH risk) calls out that Block 7 must be conditional. R-3's planner-action says: PLAN.md must "encode Block 7 as a conditional task with the decision-rule embedded." That's Task 1.

V-9 is the load-bearing gate per VALIDATION.md "if ANY path differs → STOP and investigate (visual edits leaked into JS logic)." If the visual edits in Plans 01-04 accidentally touched `calculateScore()` or any of the slide-flow / scoring functions (lines 758-961), V-9 catches it. That's Task 2.

Output: 0-2 markup edits depending on Task 1 outcome; 1 phase-end verification commit summarizing V-9 results. The phase ships green ONLY if V-9 passes on all 6 paths.

**Threat-model coverage:** Both tasks are verification checkpoints; no new code changes other than Task 1's optional typography edits. Task 1's typography edits are pure CSS class swaps (e.g., `text-5xl` → `text-6xl`; `font-bold` → `font-extrabold`) — no behavior change, no new input. T-2 register is unchanged from Plans 01-04. See `<threat_model>` below.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-overdrive-default-theme-migration/02-CONTEXT.md
@.planning/phases/02-overdrive-default-theme-migration/02-RESEARCH.md
@.planning/phases/02-overdrive-default-theme-migration/02-PATTERNS.md
@.planning/phases/02-overdrive-default-theme-migration/02-VALIDATION.md
@.planning/phases/02-overdrive-default-theme-migration/02-01-PLAN.md
@.planning/phases/02-overdrive-default-theme-migration/02-02-PLAN.md
@.planning/phases/02-overdrive-default-theme-migration/02-03-PLAN.md
@.planning/phases/02-overdrive-default-theme-migration/02-04-PLAN.md
@docs/design/design-system-alpha-overdrive.md
@index.html

<interfaces>
<!-- Typography candidate sites for R-3 Block 7 conditional edits -->
<!-- (all re-grep verified at 2026-05-16 HEAD b1af5f7) -->

If Task 1 decision = "1 markup edit":
- Line 303: slide-0 hero `<h1>` currently `text-5xl md:text-[3.5rem] font-bold`. Bump to `text-6xl font-extrabold` (or planner-discretion within Overdrive Section 4 "extreme weight contrasts" principle).

If Task 1 decision = "2 markup edits":
- Line 303: as above.
- Lines 344, 386, 421, 463, 499: slides 1-5 question headers; currently each carries `font-display text-3xl font-bold` (re-grep to confirm; sizes may vary). Bump `font-bold` → `font-extrabold` (weight-only adjustment).

V-9 6-path matrix (from VALIDATION.md):
| Path | S1 buyerUser | S2 viral | S3 scope | S4 acc | S5 fric | Expected result |
|------|--------------|----------|----------|--------|---------|-----------------|
| P1 | individual | high | individual | 4+ | 0-2 | "Pure PLG: Ideal Candidate" |
| P2 | individual | low | individual | <4 OR fric>2 | <4 OR fric>2 | "PLG Motion with Optimization Needed" |
| P3 | team | high | team | 4+ | 0-2 | "Product-Led Sales (Hybrid)" |
| P4 | csuite | (any) | (any) | 4+ | 0-2 | "Sales-Led with Wedge Opportunity" + wedge-callout VISIBLE |
| P5 | csuite | (any) | (any) | 0-3 | 3+ | "Sales-Led Growth Required" + override-notice VISIBLE |
| P6 | (mixed) | (mixed) | (mixed) | (mixed) | (mixed) | "Hybrid Approach Recommended" |
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: D-13 conditional typography decision (Block 7 / R-3)</name>
  <files>index.html (conditional — 0, 1, or 2 lines depending on decision)</files>
  <read_first>
    - index.html (re-grep line 303 hero; lines 344, 386, 421, 463, 499 slide question headers — confirm current sizes/weights before deciding)
    - .planning/phases/02-overdrive-default-theme-migration/02-CONTEXT.md (D-13 minimum-viable adjustments bound; Claude's-Discretion bullet for "0/1/2 markup edits")
    - .planning/phases/02-overdrive-default-theme-migration/02-RESEARCH.md (## Risks → R-3; ## File-Level Scope → Body markup → Slides 0-5 markup edits)
    - .planning/phases/02-overdrive-default-theme-migration/02-02-SUMMARY.md (visual notes for Block 7 — written by Plan 02 executor; describes whether Space Grotesk reads weaker than Fraunces did)
    - docs/design/design-system-alpha-overdrive.md (§4 Typography — Display sizing + extreme weight contrasts principle)
  </read_first>
  <action>Pause for user decision per the `<decision>` / `<options>` blocks below. The executor does NOT edit any file until the user replies with option-0, option-1, or option-2. If option-1 or option-2 is selected, the executor applies the corresponding edits per the `<action_if_selected>` text inside that option and commits. If option-0, the executor records the decision in SUMMARY.md with zero file changes.</action>
  <decision>
After Plans 01-04 have all landed and Space Grotesk renders end-to-end, eyeball the typography in the browser. Decide: 0, 1, or 2 markup edits?
  </decision>
  <context>
D-13 explicitly leaves this open until Space Grotesk is rendering. Space Grotesk has a more geometric / less ornamental letterform than Fraunces. The current `text-5xl md:text-[3.5rem] font-bold` slide-0 hero may read visibly weaker than Fraunces did because:
- Space Grotesk at 700 (`font-bold`) carries less stroke weight than Fraunces at 700.
- The 56px / 3.5rem hero size that worked for Fraunces may feel under-scaled for Space Grotesk's more geometric profile.

But D-13's minimum-viable bound is INTENTIONAL. The default decision is 0 edits — full typographic scale redesign is explicitly deferred (CONTEXT.md deferred-idea-2). Only adjust if the visible weakness is OBVIOUS, not subtle.

Read 02-02-SUMMARY.md's "Visual notes for Plan 05 Task 1" section first — Plan 02's executor will have left concrete observations from their browser-load of the Space-Grotesk-loaded state.
  </context>
  <options>
    <option id="option-0">
      <name>0 edits — D-13 minimum-viable default (RECOMMENDED)</name>
      <pros>Honors D-13's minimum-viable bound. No risk of over-adjusting. Phase 2 ships with the typography as-is; future polish phase can revisit.</pros>
      <cons>If Space Grotesk reads notably weaker than Fraunces did, the hero feels under-scaled. Acceptable cost per D-13.</cons>
    </option>
    <option id="option-1">
      <name>1 edit — bump slide-0 hero only (line 303)</name>
      <pros>Targets the most visible element (the welcome hero). Restores Fraunces-equivalent presence with one minimal change.</pros>
      <cons>Other display headings (slides 1-5 question headers + results page H2s) are not adjusted; potential visual inconsistency.</cons>
      <action_if_selected>Edit line 303 only. Current: `text-5xl md:text-[3.5rem] font-bold`. Target: `text-6xl md:text-[4rem] font-extrabold` (planner-discretion within Overdrive Section 4 sizing). Commit: `feat(02-05): bump slide-0 hero typography for Space Grotesk parity (D-13 / R-3)`.</action_if_selected>
    </option>
    <option id="option-2">
      <name>2 edits — bump slide-0 hero + slides 1-5 question header weights</name>
      <pros>Coherent display-font weight across all question slides; matches the Overdrive "extreme weight contrasts" principle.</pros>
      <cons>5 lines edited (lines 344, 386, 421, 463, 499). Larger diff. May over-correct if the underweight feel was mostly hero-bound.</cons>
      <action_if_selected>Edit line 303 per option-1 + change `font-bold` → `font-extrabold` on lines 344, 386, 421, 463, 499. (Re-grep each anchor before editing — multiple `font-display` headings exist; only target the question-header sites.) Commit: `feat(02-05): bump display typography weights for Space Grotesk parity (D-13 / R-3)`.</action_if_selected>
    </option>
  </options>
  <resume-signal>Select: option-0, option-1, or option-2. Reply with the option ID and a 1-sentence rationale.</resume-signal>
  <acceptance_criteria>
    - **Decision recorded:** PLAN.md is annotated with the chosen option (0/1/2). Recorded in the eventual SUMMARY.md.
    - **If option-1 or option-2:** the corresponding markup edit(s) are committed; `grep -nE 'text-6xl|font-extrabold' index.html` returns the expected match count for the chosen option.
    - **If option-0:** zero markup edits; explicit annotation in SUMMARY.md that D-13 minimum-viable bound was honored.
    - **Behavior-assertion:** After any committed edits, browser-load and confirm the chosen typography state reads coherently across all six slides + results page. No new console errors.
  </acceptance_criteria>
  <done>
The 0/1/2 markup decision is committed. If option-1 or option-2, the edits land in a single commit. Browser shows the chosen typography state without regression.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: V-9 scoring regression check + phase-end V-3/V-7 verification</name>
  <files>(no file modifications — verification + SUMMARY commit only)</files>
  <read_first>
    - index.html (re-read `calculateScore()` at line ~938 to confirm it was NOT edited — verify against git diff: `git diff main -- index.html` should show ZERO edits in lines 708-1000)
    - .planning/phases/02-overdrive-default-theme-migration/02-CONTEXT.md (EXCL-scoring-or-flow-changes; acceptance criterion #4)
    - .planning/phases/02-overdrive-default-theme-migration/02-VALIDATION.md (V-1 through V-10; especially V-9 6-path matrix + V-3 recognition test + V-7 font fallback)
  </read_first>
  <action>Pause for user verification per the `<how-to-verify>` block below. The executor does NOT mark the phase complete until the user replies "approved". If the user reports a regression / visual issue / font fallback failure / grep residue, the executor records the failure mode and stops — the orchestrator routes to gap-closure planning or `git bisect`. On approval, the executor commits the SUMMARY.md per `<output>`.</action>
  <what-built>
After Plans 01-04 (and Task 1 above) have all landed, the app should render Overdrive end-to-end:
- All 6 slides on warm off-white with Space Grotesk display + Plus Jakarta Sans body + JetBrains Mono overlines + orange accents (progress bar, CTAs, overlines, selected cards).
- Results page on warm off-white with 3 orange section dividers, white main result card with orange left-border, two callouts with orange top-strips (when triggered), Light Yellow + Golden Yellow on PLS badge + PLS card icon bg + warning icon.
- Footer on warm off-white with 4px orange top-rule.
- Zero indigo / blue / dark surfaces anywhere.
- Scoring + flow + copy unchanged: same 6 slides, same answer→result decision tree.
  </what-built>
  <how-to-verify>
**Step 1 — Open browser:**
1. `python3 -m http.server 8080` from repo root.
2. Open `http://localhost:8080/` in Chrome (hard reload `Cmd+Shift+R` to bypass cache).
3. Open DevTools console — confirm zero red errors at load. (One expected pre-existing Tailwind CDN production WARNING is OK — same as Phase 1 baseline + V-1 run log.)

**Step 2 — V-9 scoring regression matrix (the load-bearing gate per VALIDATION.md):**
Walk each of the 6 paths below. For each, confirm the result string + callout visibility match the expected behavior. **If ANY path differs from expected, STOP and investigate** — visual edits may have leaked into JS logic (failure of acceptance criterion #4 + EXCL-scoring-or-flow-changes).

| Path | S1 buyerUser | S2 viral | S3 scope | S4 acc count | S5 fric count | Expected result | Wedge callout | Override notice |
|------|--------------|----------|----------|--------------|---------------|-----------------|---------------|-----------------|
| P1 | individual | high | individual | 4+ | 0-2 | "Pure PLG: Ideal Candidate" | hidden | hidden |
| P2 | individual | low | individual | <4 OR fric>2 | (matching) | "PLG Motion with Optimization Needed" | hidden | hidden |
| P3 | team | high | team | 4+ | 0-2 | "Product-Led Sales (Hybrid)" | hidden | hidden |
| P4 | csuite | any | any | 4+ | 0-2 | "Sales-Led with Wedge Opportunity" | **VISIBLE** | hidden |
| P5 | csuite | any | any | 0-3 | 3+ | "Sales-Led Growth Required" | hidden | **VISIBLE** |
| P6 | (mixed — non-PLG, non-csuite, mid acc/fric) | mixed | mixed | mixed | mixed | "Hybrid Approach Recommended" | hidden | hidden |

After each path, click "Take quiz again" (restart) to reset and walk the next path. **Tally:** all 6 paths produce expected result + correct callout visibility. **If any path diverges → STOP, do NOT mark phase complete — investigate which Plan introduced the regression by `git bisect` between Phase 1 SUMMARY commit and HEAD.**

**Step 3 — V-3 recognition test (visual coherence per design system §9):**
Walk the quiz one more time. Cover the page title with your hand or a sticky note. Does the page still feel like Overdrive? Concrete checks:
- (a) Warm off-white bg on every slide + results + footer. No interruptions.
- (b) Orange visible structurally: progress bar, "Question N of 5" overlines, CTAs, selected radio/checkbox cards, all 3 results-page section dividers, main result card 3px left-border, both callout top-strips (P4 + P5 paths), footer top-rule.
- (c) Space Grotesk on every `.font-display` element (slide-0 hero, slides 1-5 question headers, results page H2s).
- (d) Plus Jakarta Sans body, JetBrains Mono overlines.
- (e) Light Yellow + Golden Yellow on PLS badge + PLS card icon bg + override-notice warning icon.
- (f) Zero indigo / purple / blue surfaces.
- (g) Zero dark surfaces.
- (h) Recognition test passes — covered title, page still reads as Overdrive.

**Step 4 — V-7 font-load + fallback (re-verify):**
1. DevTools → Network tab → Throttling = "Offline". Hard reload. Page renders with `sans-serif` fallback. Readable. Not broken.
2. Reset throttling to "No throttling". Hard reload. Confirm `space-grotesk-*.woff2` files load from `fonts.gstatic.com`.

**Step 5 — Phase-end grep sweep:**
Run all three residue greps:
- `grep -nE '#0F172A|#1E293B|bg-surface-dark|bg-surface-dark-card|bg-slate-(7|8|9)00|--surface-dark-rgb|--surface-dark-card-rgb' index.html | grep -v '/\*'` → expect 0 matches.
- `grep -nE '#4F46E5|#4338CA|#EEF2FF|#A5B4FC|#818CF8|#F8F7FF|Fraunces|bg-indigo|text-indigo|hover:bg-indigo|hover:text-indigo' index.html` → expect 0 matches.
- `grep -nE 'style="[^"]*#[0-9A-Fa-f]{3,6}"' index.html` → expect 0 matches.

**Step 6 — calculateScore() integrity check:**
Run `git diff main -- index.html` and filter to the script region:
- `git diff main -- index.html | grep -E '^[+-]' | grep -E 'function (calculateScore|renderCheckboxes|goTo|updateChrome|selectAnswer|showResults|restartQuiz)' | wc -l`
- Expected: 0 matches (no function signatures changed).
- Manual scan: look at any `+`/`-` lines in the diff that fall in the line range ~708-1000 (the `<script>` block). Any non-comment edits to this region are a RED FLAG — Plan 02 should NOT have touched scoring/flow JS.
  </how-to-verify>
  <resume-signal>
Type "approved" if all 6 V-9 paths pass + V-3 recognition test passes + V-7 font fallback passes + all grep sweeps return 0 matches + calculateScore() is unchanged.

Type "regression — path PN" (where N = 1-6) if any V-9 path fails. The orchestrator will route to `git bisect` or revert.

Type "visual issue — <describe>" if V-3 recognition test fails. Specify which element / criterion (a-h) failed.

Type "font fallback broken" if V-7 Offline test fails.

Type "grep residue — <which>" if any phase-end grep returns matches. The orchestrator will route to gap-closure planning.
  </resume-signal>
  <acceptance_criteria>
    - **V-9 (acceptance criterion #4 + EXCL-scoring-or-flow-changes):** All 6 paths produce expected result string + callout visibility. NO divergence from pre-Phase-2 baseline.
    - **V-3 (acceptance criterion #1 + #3):** All 8 sub-criteria (a-h) pass; recognition test passes.
    - **V-7 (acceptance criterion #1):** Space Grotesk loads from `fonts.gstatic.com`; Offline fallback renders readable text.
    - **V-4 + V-5 + V-8 (residue greps):** All return 0 matches.
    - **calculateScore() integrity:** zero edits to any JS function — confirmed by diff inspection.
    - **D-14 phase-end disposition:** All `<head>`-touching commits (Plans 01 Task 1, 01 Task 2, 02 Task 1, 03 Task 1, 03 Task 2) have their corresponding V-2 sub-rows passed individually (NOT a single end-of-phase sweep — that was the entire point of D-14). The phase-end V-7 + V-3 are additional coherence checks, not replacements for per-commit V-2 verifies.
  </acceptance_criteria>
  <done>
User typed "approved." All 4 acceptance criteria from ROADMAP.md Phase 2 success criteria pass: (1) Overdrive identity end-to-end, (2) dark hero gone with orange-as-structure carrying contrast, (3) entire app internally coherent, (4) quiz behavior + scoring + copy unchanged. Phase 2 is shippable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ⇄ static HTML | No change from prior plans. |

## STRIDE Threat Register (per SECURITY_CFG=true, ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none new) | — | — | — | This plan introduces no new code paths. Task 1's optional typography edits are pure Tailwind class swaps (`text-5xl` → `text-6xl`, `font-bold` → `font-extrabold`) — no behavior change, no new input handling. Task 2 is purely verification. T-2-01 through T-2-05 from Plans 01-04 remain accepted/N-A. |

**Block-on-high disposition:** No new high-severity threats. Block-on-high gate clears.
</threat_model>

<verification>
This entire plan IS the phase-end verification. Both tasks are checkpoints — Task 1 pauses for user decision, Task 2 pauses for user-verified result. If either fails, the phase does not ship.

If Task 1's edits introduce console errors or visible regression: revert and re-decide.

If Task 2 finds any V-9 path divergence: STOP. Per VALIDATION.md V-9 gate language: "If ANY path differs → STOP and investigate (visual edits leaked into JS logic)." Route to gap-closure plan or git bisect.
</verification>

<success_criteria>
1. Task 1 decision (0/1/2 markup edits) is recorded and any edits are committed.
2. Task 2 V-9 all 6 paths pass.
3. Task 2 V-3 visual coherence passes (all 8 sub-criteria).
4. Task 2 V-7 font fallback passes.
5. Phase-end grep sweep returns 0 matches across all three residue patterns.
6. `calculateScore()` and all other scoring/flow JS functions are byte-identical to pre-Phase-2 baseline.
7. User typed "approved" at Task 2 checkpoint.

The 4 ROADMAP acceptance criteria for Phase 2:
- AC#1: Overdrive identity end-to-end (V-3) ✓
- AC#2: Dark hero gone with orange-as-structure (V-4 + V-10 + V-3 sub-b) ✓
- AC#3: Entire app internally coherent (V-3 + V-5) ✓
- AC#4: Quiz behavior unchanged (V-9) ✓

All four passing = Phase 2 ships.
</success_criteria>

<output>
After completion, create `.planning/phases/02-overdrive-default-theme-migration/02-05-SUMMARY.md` documenting:
- Task 1 decision (0/1/2 markup edits) + rationale + any committed diff.
- Task 2 V-9 6-path result table (each path → actual result string + callout visibility).
- V-3 recognition test result (8 sub-criteria pass/fail).
- V-7 font fallback result.
- Phase-end grep sweep results (each of 3 greps + match count).
- calculateScore() integrity confirmation (git diff snippet).
- Phase 2 sign-off: all 4 ROADMAP acceptance criteria status.
- Carry-over to Phase 3: Plan 3 needs to build a `[data-theme="alchemist"]` override block on the Overdrive default — the contract structure is locked, the next phase's job is to demonstrate per-client pluggability per REQ-stub-second-theme. Plan 04 verified that `bg-surface-elev`, `bg-yellow-100`, `text-yellow-400` are wired; Phase 3's override block will exercise these AND override `--accent-rgb` / `--accent-hover-rgb` / etc. to a distinct palette.

After SUMMARY commits, the orchestrator merges `rebrand-theming` into `main` (or whatever ship-target was agreed) per Pete's git discipline (worktree workflow if applicable; PR if branch workflow).
</output>
</content>
</invoke>